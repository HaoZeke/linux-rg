#+title: linux-rg patch and policy candidates

* Overview

This file documents carried modules, pending patch candidates, and userspace
policy options for linux-rg. It is the shared candidate registry.

Literature grades refer to =docs/literature-review.org= (including "Why kernel
research feels absent" and the venue map -- read that before trusting
patch-stack marketing).

** Applicability

Each candidate records which profiles it is relevant to, carried for, or
blocked on. Use these markers:

- =applies: rgx1gen11, rgam5terra= -- evidence or motivation exists for these profiles
- =carry: rgx1gen11= -- currently enabled in that profile's .config overlay
- =blocked: rgam5terra= -- evaluated and rejected (with reason)

A patch in the shared 00xx series may be compiled in for one profile and left
off (or made a module) for another.

** Carry rules (per-profile)

- Public, reviewable source.
- Reproducible application from the PKGBUILD.
- Clean compile against the current Arch base.
- Documented behavior match or measured win for the target profile.
- Disable or modularize when practical.

See the profile's kernel.org and targets.org for the final carried set and
acceptance criteria (e.g. =docs/rgx1gen11-kernel.org=).

** Current high-level patch applicability (summary)

This is a convenience view. The authoritative per-candidate tables live under
each carried / candidate section.

| Patch / area | rgx1gen11 | rgam5terra | Notes |
|--------------|-----------|------------|-------|
| BORE + arch-adapt | carried | evaluating | Laptop hybrid vs homogeneous Zen 5 CCDs |
| BBRv3 + arch-adapt | carried | evaluating | Laptop roam vs desktop LAN+VPN |
| v4l2loopback | carried | likely | Virtual cam utility |
| DDCCI | carried | blocked/low | Desktop: ddcutil userspace preferred |
| Block contention (0007) | carried | evaluating | SN850X under build + browser |
| ADIOS (module) | carried | evaluating | SN850X A/B; same HOTSTORAGE gate |
| 0010-0013 sched weld | carried | blocked | Intel P/E motivation; see kernel.org |
| 0014-0015 zen MM/cpufreq | partial | evaluating | 0014 MM yes; 0015 schedutil path differs |
| 0016 cpuidle TEO | carried | blocked | Intel idle governor |
| ASA sched_ext router | carried | evaluating | Strong desktop expert-switching case |
| PSI/MGLRU/zswap/zram/DAMON | carried | planned | 48 GiB sizing; see targets.org |
| Cache-aware sched (0020) | carried | evaluating | **Higher priority** on dual-CCD 9950X |
| MM bulk-free (0021) | carried | evaluating | Large-RAM reclaim paths |
| Hybrid iwlwifi/i915 zen hunks | skipped | n/a | Wrong hardware class |
| 0022 znver5 RDSEED | n/a | planned | Zen 5 only (cachyos-fixes #19) |
| 0023 btrfs DIO | n/a | candidate | btrfs root option |
| 0024 MZEN5 ISA | n/a | evaluating | graysky; not znver4 |
| 0025 gaming-sched | n/a | evaluating | 32T cgroup fairness |
| 0026 newidle revert | n/a | evaluating | alt to 0025 |
| 0027 BT USB quirks | n/a | probe-gated | cachyos-fixes #04 delta |
| 0028 in-kernel r8125 | n/a | probe-gated | alt to DKMS |
| 0029 udmabuf sg fix | shared? | candidate | cachyos-fixes #11 |
| 0040 poc-selector dual-CCD | n/a | evaluating | sirlucjan |
| 0042 NAP cpuidle | n/a | evaluating | amd idle ML gov |
| T41-T44 cmdline matrix | n/a | probe | AMD idle/IOMMU/NVMe |
| T45-T47 NVIDIA modparams | n/a | probe | DPM/PAT/fan |
| T48-T49 uclamp | shared | planned | cgroup slices + Slurm |
| T54 rg-terra-power | n/a | planned | chezmoi thermal profiles |

rgam5terra research pass 2026-06-19: profile docs populated from primary
sources; live carry decisions remain gated on assembly + bench (=linux-rg-3up1=).

Downstream survey (CachyOS / sirlucjan / Liquorix): =docs/rgam5terra-kernel.org=
("Downstream kernel survey"). Hardware tweak registry (T1-T26): same file.

* Candidate: amd znver5 RDSEED warn-only (0022)

** Status

| Field | Value |
| Decision | profile-gated carry for rgam5terra build |
| Evidence grade | B (CPU erratum + microcode policy) |
| Source | cachyos-fixes-patches-v27 #19/46; Eric Naim |
| Patch file | =0022-amd-znver5-rdseed.patch=; PKGBUILD applies it only for =LINUX_RG_PROFILE=rgam5terra= |
| vissue | =linux-rg-gjou= |

** What it is

On Zen 5, mainline =init_amd_zen5()= clears =X86_FEATURE_RDSEED= when microcode
is below the known-good revision. CachyOS changes this to **warn only** so
RDSEED remains available while nagging to update =amd-ucode=.

** Applicability

| Profile | Status |
| rgx1gen11 | n/a (Intel) |
| rgam5terra | profile-gated carry |

** Carry gate

Carry only in the rgam5terra profile. Production box must still install =amd-ucode=
and pass early microcode load in initramfs.

* Candidate: btrfs DIO parallel writes (0023)

** Status

| Field | Value |
| Decision | candidate (btrfs root only) |
| Evidence grade | B/C (filesystem maintainer fix; Cachy measured ~59% DIO on one box) |
| Source | cachyos-fixes-patches-v27 #38/46 |
| vissue | =linux-rg-jt9d= |

** What it is

Restores btrfs behavior where direct I/O writes to the same file were wrongly
serialized after the new mount API transition. One-line =IS_NOSEC= path fix in
btrfs.

** rgam5terra fit

Relevant if root or data subvolumes use btrfs (likely, mirroring rgx1gen11).
Irrelevant for ext4-only installs.

** Decision gate

Carry only when root FS is confirmed btrfs. **Arch 7.0.12 already has =IS_NOSEC=
parallel DIO path** in =fs/btrfs/direct-io.c= (downstream survey 2026-06-19) --
likely CANCELLED unless rebased kernel regresses.

* Candidate: graysky MZEN5 / znver5 compiler target (0024)

** Status

| Field | Value |
| Decision | evaluating |
| Evidence grade | D (ISA tuning folklore) |
| Source | sirlucjan =7.0/cpu-patches= (graysky merge) |
| vissue | =linux-rg-1btr= |

** What it is

Adds =CONFIG_MZEN5= selecting =-march=znver5= and =KBUILD_RUSTFLAGS -Ctarget-cpu=znver5=.
Supersedes Cachy =MZEN4= / =znver4= paths used in CachyOS build scripts today.

** Applicability

| Profile | Status |
| rgx1gen11 | blocked (Intel) |
| rgam5terra | evaluate after T1 PPT thermal cap |

** Decision gate

Same as znver5 ISA row: >1% =make -j32= or kernel compile win at identical power cap.

* Candidate: gaming-sched cgroup_mode (0025)

** Status

| Field | Value |
| Decision | evaluating |
| Evidence grade | D (Intel maintainer prototype; no published desktop study) |
| Source | sirlucjan =7.0/gaming-sched= (Peter Zijlstra series) |
| vissue | =linux-rg-g2nl= |

** What it is

Adds =cgroup_mode= sysfs modes (=smp=, =up=, =max=, =concur=) altering how
=cfs_rq= group shares are computed on many-CPU systems. Default in series: **up**
(full shares per CPU, fixes 1/n_cpus shrink).

** rgam5terra fit

9950X has 32 logical CPUs; chezmoi-style =browser.slice= / =build.slice= memory
cgroups may starve under stock proportional group shares. This is the **Cachy
desktop problem** on many-core, but via cgroup math not hybrid P/E.

** Conflicts

Touches =kernel/sched/fair.c=; stacks poorly with BORE, newidle revert (0026),
and CONFIG_CACHY fair. Try sched_ext + ASA first.

** Decision gate

Carry only if PSI/cgroup metrics show persistent build.slice CPU starvation on
stock BORE+sched_ext, and gaming-sched wins A/B over newidle revert.

* Candidate: proportional newidle balance revert (0026, rgam5terra re-eval)

** Status

| Field | Value |
| Decision | evaluating (rgam5terra only) |
| Evidence grade | D |
| Source | cachyos-fixes-patches-v27 #05/46 |
| vissue | =linux-rg-nc54= |

** rgam5terra difference from rgx1gen11 block

Original block cited hybrid CPU + BORE conflict. On 9950X the motivation is
**many-core desktop latency**, same as CachyOS intent, not P/E clusters.

** Decision gate

Mutually exclusive experiment track with 0025 gaming-sched. Do not stack with
both BORE and 0025.

* Candidate: out-of-tree r8125 driver (CachyOS packaging pattern)

** Status

| Field | Value |
| Decision | evaluating (userspace/DKMS, not kernel patch) |
| Evidence grade | C (CachyOS ships it; Realtek vendor driver) |
| Source | CachyOS =_build_r8125=yes=, github.com/aravance/r8125 |
| vissue | =linux-rg-lvi1= (DKMS); =linux-rg-awbh= (in-kernel 0028) |

** What it is

CachyOS builds Realtek's =r8125= out-of-tree module instead of in-tree =r8169=
for 2.5G Dragon LAN on AMD boards.

** rgam5terra fit

ASRock B650I Lightning WiFi advertises 2.5G Realtek. If =r8169= shows downshift
to 100M or link flaps (ArchWiki reports), mirror CachyOS and package r8125-dkms
for rgam5terra.

** Decision gate

Probe =ethtool= link modes on first boot. Carry DKMS helper only on failure.

* Candidate: BT USB quirk delta (0027)

** Status

| Field | Value |
| Decision | probe-gated |
| Evidence grade | C (board-specific BT firmware load) |
| Source | cachyos-fixes-patches-v27 #04 (+15 IDs incl. =13d3:3625=) |
| vissue | =linux-rg-eamh= |

** What it is

Adds MediaTek/Realtek BT USB device-table quirks missing from Arch 7.0.12
=btusb.c=. v27 #03 (=0489:e112= ASUS PRIME B650M) is already upstream.

** rgam5terra fit

B650I Lightning WiFi combo module VID/PID unknown until =linux-rg-ax5w= probe.
Carry only on confirmed gap + BT firmware failure.

* Candidate: in-kernel r8125 module (0028)

** Status

| Field | Value |
| Decision | probe-gated (alternative to DKMS) |
| Evidence grade | C (vendor driver; sirlucjan packaging) |
| Source | sirlucjan =7.0/r8125-patches/= (7 patches, r8169 coexist) |
| vissue | =linux-rg-awbh= |

** What it is

In-tree Realtek r8125 driver series instead of CachyOS DKMS packaging
(=linux-rg-lvi1=). Pick one path after =ethtool= probe on B650I 2.5G LAN.

* Candidate: udmabuf malformed scatterlist fix (0029)

** Status

| Field | Value |
| Decision | candidate |
| Evidence grade | B (dma-buf stability) |
| Source | cachyos-fixes-patches-v27 #11 |
| vissue | =linux-rg-6rl5= |

** What it is

Replaces =sg_set_folio= loop with =sg_alloc_table_from_pages= in udmabuf path.
Arch 7.0.12 still uses older pattern; Cachy 7.1.1 prebuilt has fix.

** Applicability

May benefit all profiles if carried in shared 00xx queue.

* Candidate: CachyOS CONFIG_CACHY fair (explicit block for rgam5terra)

** Status

| Field | Value |
| Decision | blocked |
| Evidence grade | D |
| Source | CachyOS =_cachy_config=yes= enables CONFIG_CACHY fair slice/migration |

** Why blocked

Same region as BORE and optional 0025/0026. Cachy lowers =sysctl_sched_base_slice=
and =sysctl_sched_migration_cost= when CONFIG_CACHY=y. linux-rg uses BORE for
burst scoring instead.

* Candidate: CachyOS default performance governor (explicit block)

** Status

| Field | Value |
| Decision | blocked on rgam5terra |
| Evidence grade | C (distro practice) |

Cachy PKGBUILD can set =CPU_FREQ_DEFAULT_GOV_PERFORMANCE=. **Reject** on Terra:
conflicts with T1 PPT cap and NH-L9a thermal envelope. Use amd-pstate EPP.

* Candidate: rgam5terra PPT / thermal cap (NH-L9a + Terra)

** Status

| Field | Value |
| Decision | planned (userspace/firmware; mandatory before kernel bench) |
| Evidence grade | B (Noctua TDP class) + C (SFF practice) |
| vissue | =linux-rg-1s03= |

** What it is

Cap AMD package power (PPT) to 95-120 W via BIOS or =ryzenadj= so the 9950X is
not thermally starved on an NH-L9a in a Fractal Terra with an uncapped RTX 5070
neighbor.

** Applicability

| Profile | Status | Notes |
| rgx1gen11 | n/a | Different thermal envelope (ultrabook DPTF) |
| rgam5terra | planned | **Gate** for all CPU scheduler and =-j32= benchmarks |

** Decision gate

Do not compare BORE/cache-aware/BORE sched_ext until PPT policy is documented and
CPU peak temp is stable across a 10-minute mixed load with Terra panels on.

* Candidate: rgam5terra PCIe ASPM policy (NVIDIA dGPU)

** Status

| Field | Value |
| Decision | probe (cmdline A/B) |
| Evidence grade | C (Arch/community NVIDIA+ASPM anecdotes) |
| vissue | =linux-rg-6dpc= |

** What it is

Kernel cmdline =pcie_aspm.policy= controls PCIe link power states. rgx1gen11
carries =powersave= for Iris Xe + CNVi battery idle (grade C laptop practice).

** rgam5terra fit

Desktop with ASUS RTX 5070 Dual on B650I PCIe 4.0 x16 slot through Terra riser: ASPM interactions with
NVIDIA open modules and resume are **not** transferable from the laptop profile.
Test =off= vs =powersave= with five S3 cycles and modeset recovery.

| Profile | Status | Notes |
| rgx1gen11 | carried cmdline | Ultrabook powersave |
| rgam5terra | probe | Do not inherit rgx1gen11 boot-check token |

* Candidate: rgam5terra SN850X APST policy

** Status

| Field | Value |
| Decision | probe-first (may disable) |
| Evidence grade | B (ArchWiki APST semantics) + C (WD community reports) |
| vissue | =linux-rg-gkns=, =linux-rg-rt1t= |

** What it is

NVMe autonomous power state transition (APST) saves idle power but can break
resume on some firmware (ArchWiki "broken APST support"). rgx1gen11-nvme-check
**requires** APST enabled for KIOXIA XG8.

** rgam5terra fit

SN850X on AM5 desktop (AC-powered, SFF thermals): prefer **resume stability** over
idle NVMe power savings until five suspend cycles pass with APST on. Fallback:
=nvme_core.default_ps_max_latency_us=0= or udev APST disable (grade B workaround).

| Profile | Status | Notes |
| rgx1gen11 | APST required in checker | Laptop battery + XG8 path |
| rgam5terra | probe-first | Opposite default until SN850X proves clean |

* Candidate: znver5 / x86-64-v4 ISA tuning (rgam5terra only)

** Status

| Field | Value |
| Decision | evaluating (no carry until bench) |
| Evidence grade | D (Phoronix/Cachy folklore) |
| Source | cpu-cachyos-patches lineage; Arch =-march=x86-64-v3= default |
| vissue | =linux-rg-1btr= |

** What it is

Compiler ISA selection (=znver5=, =x86-64-v4=) via Kconfig/Makefile CPU tuning
patches. Cachy ships =znver4= paths -- **wrong generation** for 9950X.

** Applicability

| Profile | Status | Notes |
| rgx1gen11 | blocked znver4 | Intel CPU |
| rgam5terra | evaluating | Bench only after T1 PPT cap fixed |

** Decision gate

Promote only if kernel image compile + =make -j32= throughput improves >1% at
**identical** PPT/thermal envelope vs Arch generic v3.

* Candidate: NVIDIA SFF power limit (RTX 5070 Dual)

** Status

| Field | Value |
| Decision | planned userspace experiment |
| Evidence grade | C (ArchWiki NVIDIA tips; not peer-reviewed) |
| vissue | =linux-rg-t49m= |

** What it is

=nvidia-smi -pl <W>= reduces GPU power draw and heat dump into Terra. Pairs with
T1 CPU PPT cap for closed-panel thermal-sustained profile.

Not a kernel patch; document in profile targets and =rgam5terra-thermal-check=
(future).

* Carried: ADIOS I/O scheduler

** Status

| Field | Value |
| Decision | carried (module, non-default) |
| Evidence grade | D (no peer-reviewed evaluation) |
| Patch | =0008-adios-cachy.patch= (extracted from CachyOS 6.17 omnibus) |
| Config | =CONFIG_MQ_IOSCHED_ADIOS=m=, =# CONFIG_MQ_IOSCHED_DEFAULT_ADIOS is not set= |

** What it is

ADIOS (Adaptive Deadline I/O Scheduler) is a multi-queue block scheduler for
=blk-mq= devices. It combines deadline scheduling with adaptive latency
control: it learns completion latency by operation type and size, predicts
completion time for new requests, and adjusts deadlines and batching within a
configurable global latency window.

Primary reference: https://github.com/firelzrd/adios

Version carried in the 7.0 extraction: 3.1.7 (CachyOS omnibus). Upstream ADIOS
3.2.0 exists for newer kernels via firelzrd stable patches; revisit on kernel
bumps.

** Applicability

| Profile     | Status     | Notes |
|-------------|------------|-------|
| rgx1gen11   | carried    | KIOXIA XG8 ULL NVMe under mixed laptop load |
| rgam5terra  | evaluating | WD SN850X 2 TB; SFF thermal envelope; same HOTSTORAGE gate |

** rgam5terra fit (research 2026-06-19)

SN850X is PCIe Gen4 ULL class storage under the same HOTSTORAGE 2023 skepticism
as KIOXIA XG8. The desktop differs by sustained power (build -j32, less idle on
battery) and case thermals (Fractal Terra + NH-L9a). BFQ stays the conservative
default; ADIOS remains a module for local A/B only.

Suggested SN850X measurement (after assembly):

#+begin_src sh
sudo modprobe adios
cat /sys/block/nvme0n1/queue/scheduler
for s in bfq adios none; do
  echo "$s" | sudo tee /sys/block/nvme0n1/queue/scheduler
  # fio mixed randrw + background desktop load; record p99
done
#+end_src

** rgx1gen11 fit

The ThinkPad uses a KIOXIA XG8 NVMe device under mixed interactive and build
load: browser caches, source trees, package builds, VMs, and sync tools share
one storage device.

BFQ remains the default because it has decades of published scheduler research
and is a conservative laptop default. ADIOS is carried as a module so block
scheduler choice can be tested without another DKMS or out-of-tree drift path.

** Literature context

- BFQ: Valente and Avanzini (ACM OSR 2012); evolution paper (Unimore).
- HOTSTORAGE 2023 (Whitaker et al.): on ultra-low-latency NVMe, mq schedulers
  often do not improve performance or energy efficiency and can increase
  latency. See =docs/literature-review.org=.

ADIOS has no peer-reviewed evaluation. Carrying it is an engineering option for
local measurement, not a literature-mandated addition.

** Relationship to other block choices

| Component | Role |
| BFQ (default) | cgroup-aware fairness; literature-backed for mixed workloads |
| Block contention patch | Reduces merge and dispatch lock contention in BFQ and mq-deadline |
| ADIOS (module) | Optional adaptive-deadline scheduler for A/B testing |

Do not assume ADIOS plus BFQ contention patch plus BFQ default is optimal.
HOTSTORAGE 2023 motivates comparing against =none= on this NVMe.

** How to test on the laptop

Switch scheduler on the NVMe queue (replace =nvme0n1= if needed):

#+begin_src sh
# show current scheduler
cat /sys/block/nvme0n1/queue/scheduler

# try ADIOS (module must be loaded)
sudo modprobe adios
echo adios | sudo tee /sys/block/nvme0n1/queue/scheduler

# revert to BFQ
echo bfq | sudo tee /sys/block/nvme0n1/queue/scheduler

# literature-motivated baseline from HOTSTORAGE 2023
echo none | sudo tee /sys/block/nvme0n1/queue/scheduler
#+end_src

Suggested measurement set:

1. Desktop feel: Sway, browser, editor, background =makepkg= or kernel build.
2. Storage latency: =fio= random read/write mixed workload on =/home=.
3. Energy: =powertop= or RAPL-backed logging during idle and mixed load.

Record: scheduler name, latency percentiles, interactive stutter subjectively,
and whether writeback flusher behavior changes under memory pressure.

** Sysfs tunables

Under =/sys/block/<dev>/queue/iosched/= when ADIOS is active:

- =adios_version= (read-only)
- =global_latency_window= (nanoseconds; lower tends to reduce latency)
- =bq_refill_below_ratio= (percentage of global window)
- =batch_actual_max= (read-only per operation type)

Start with defaults; tune only after baseline captures.

** Carry tradeoffs

- Adds =block/adios.c= (~2000 lines) and Kconfig integration.
- Conflicts possible with future Arch block-layer changes on rebases.
- Making ADIOS the default (=CONFIG_MQ_IOSCHED_DEFAULT_ADIOS=y=) is not
  literature-supported; keep BFQ default until local NVMe study completes.

** Decision gate for promoting to default

Promote ADIOS to default only if, for a given profile:

- Interactive latency is consistently better than BFQ and =none= across repeated
  trials, and
- No regression in package build throughput or suspend/resume stability, and
- Energy is not worse than =none= at idle (HOTSTORAGE 2023 criterion).

* Blocked fallback: re-swappiness v1.2

** Status

| Field | Value |
| Decision | blocked fallback (not carried) |
| Evidence grade | D (patch); A (problem framing via ATC 2020 and SWAM) |
| Source | sirlucjan =7.0/re-swappiness-patches= |
| Upstream project | https://github.com/firelzrd/re-swappiness |

** What it is

re-swappiness replaces global swappiness semantics with MGLRU-aware reclaim
behavior. It adjusts how aggressively anonymous and file-backed pages are
scanned under pressure, using per-type generation tracking in the multi-gen LRU.

Touched files (typical 7.0 patch):

- =include/linux/mm_inline.h=
- =include/linux/mmzone.h=
- =mm/vmscan.c=
- =mm/workingset.c=

** rgx1gen11 motivation (example)

rgx1gen11 enables MGLRU, zswap, zram, KSM, PSI, and DAMON. Under browser plus
build plus container load, reclaim policy strongly affects responsiveness.

Literature motivation:

- Lebeck et al., USENIX ATC 2020: default mobile memory management
  (swappiness and OOM killing) is often misaligned with responsiveness goals.
- Lim et al., SWAM (arXiv:2306.08345): revisiting swap and OOM killer policy
  for application responsiveness.

re-swappiness is a downstream implementation in that lineage, not an ATC 2020
or SWAM paper result itself.

** Risks and interactions

- Overlaps =mm/vmscan.c= with MGLRU mainline code and with the carried
  =mglru-patches-v2= patch. Do not stack without diff review.
- May interact with zram writeback and zswap pool pressure; test with the same
  userspace memory policy intended for production.
- No Lenovo or ThinkPad-specific validation found.

** Evaluation plan before carry

1. Baseline: current linux-rg with production zram/zswap systemd units.
2. Measure under fixed workload: browser tabs, =make -j9= kernel build, optional
   VM or container.
3. Capture: =/proc/pressure/memory=, swap in/out rates, OOM or near-OOM events,
   time-to-complete build, desktop latency subjectively.
4. Compare against mainline swappiness and =vm.*= tunables only (no patch).

** Decision gate

Carry only if responsiveness improves measurably without increasing swap thrash
or build time regression on repeated runs.

* Carried: cpuidle TEO over menu governor (0016)

** Status

| Field | Value |
| Decision | **carried** (=0016-cpuidle-teo-default.patch=) |
| Evidence grade | B |
| Source | sirlucjan =7.0/cpuidle-patches= (Christian Loehle upstream patch) |
| vissue | =linux-rg-7m20= (runtime validation via bench-idle) |

** What it is

Small Kconfig and governor rating change:

- Prefer =CPU_IDLE_GOV_TEO= over =CPU_IDLE_GOV_MENU= when =NO_HZ= or
  =NO_HZ_IDLE= is enabled.
- Lowers menu governor rating below TEO so TEO wins selection.

Files:

- =drivers/cpuidle/Kconfig=
- =drivers/cpuidle/governors/menu.c=
- =drivers/cpuidle/governors/teo.c=

** rgx1gen11 motivation (example)

rgx1gen11 uses =intel_idle=, =intel_pstate=, and =schedutil= on a Raptor Lake
U-series laptop where s2idle and NO_HZ idle are active paths.

TEO (Timer Events Oriented governor) focuses on predictable timer-driven idle
state selection. Menu's interactivity boost is less decisive on modern kernels;
upstream merged TEO preference on that basis.

** Live probe (2026-06-18)

Stock Arch 7.0.12 on the bench selects **menu** at runtime
(=/sys/devices/system/cpu/cpuidle/current_governor=), despite building TEO.
Patch is not redundant.

** Post-boot validation (=linux-rg-bench-idle=)

1. Confirm governor reads =teo= after linux-rg boot.
2. Compare C-state residency vs stock menu baseline (powertop / turbostat).
3. Ten s2idle cycles: no resume regression.
4. Gate promotion: equal or better idle power; no fan spin-up regression undocked.

* rgx1gen11 hardware fit (example profile)

Reference machine: ThinkPad X1 Carbon Gen 11, i7-1365U, 32 GiB RAM, Iris Xe
(8086:a7a1), CNVi Wi-Fi (8086:51f1), KIOXIA XG8 1 TB NVMe, dual TB4.

Full inventory and measurable targets: =docs/rgx1gen11-targets.org=.

** High-fit carries (kernel)

| Area | Why on this box | Patches / config |
| Hybrid scheduler | 2P+8E, 12 threads, SMT | BORE, 0010-0013, sched_ext, ASA |
| NVMe responsiveness | Single ULL SSD, mixed desktop+build | BFQ 0007, ADIOS module 0008 |
| Roaming network | CNVi, travel profile | BBRv3 0003/0004 |
| Memory pressure | 32 GiB but heavy zram today | PSI, MGLRU, zswap, zram, DAMON |
| Docked display | TB4 + DDCCI | 0006 DDCCI |
| DKMS avoidance | Custom kernel cadence | v4l2loopback 0005 |

** Hold / skip on this box

| Candidate | Reason |
| CONFIG_CACHY fair / MUQSS | BORE stack; wrong maintenance model |
| Panther Lake i915 PSR hunks | Raptor Lake Iris Xe panel IP |
| znver4 / x86-64-v3 ISA | Intel CPU |
| cgroup VRAM, rt-i915 | iGPU laptop; patch or config mismatch |
| handheld, aufs, acpi_call | Wrong platform class |
| cache-aware SCHED_CACHE | Carried as =0020=; benchmark against sched_ext userspace |

** Wi-Fi / BT on this box (verified PCI)

| Device | PCI/USB | Stack | Boot-safe? |
| AX211 Wi-Fi | 8086:51f1 CNVi | iwlmvm, iwlwifi-ma-b0 firmware | =m= modules; not in initramfs |
| AX211 BT | 8087:0033 USB | btusb, btintel | =m= modules; not in initramfs |

Kernel pins: =IWLMLD=, =IWLWIFI_LEDS=, =BT_HCIBTUSB_AUTOSUSPEND=, =bt_coex_active=
modprobe. Full map: =docs/rgx1gen11-drivers.org=. Boot rules:
=docs/rgx1gen11-boot-safety.org=.

** Userspace-first on this box

| Policy | Vissue |
| zram sizing for 32 GiB | =linux-rg-zram= |
| systemd-oomd PSI thresholds | =linux-rg-oomd= |
| scx_loader laptop modes | =linux-rg-tjsl= |
| i915 vs xe boot default | =linux-rg-i915= |
| cpuidle governor check | =linux-rg-7m20= |
| iwlwifi power_save A/B | =linux-rg-iwl= |
| BT resume after s2idle | =linux-rg-bt= |

* Candidate bundle: cachyos-fixes-v27 hot-path extractions

** Status

| Field | Value |
| Decision | partial carry |
| Evidence grade | B for upstream-reviewed hunks; D as bundle |
| Source | sirlucjan =7.0/cachyos-fixes-patches-v27= (46 patches; cherry-pick subset) |

** High-value hunks for rgx1gen11

| # | Subject | Files | Status |
| 06-08 | hot-path inlines | tlb.c, sched.h, core.c | **carried** =0010= |
| 09 | =detach_tasks()= loop_max fix | kernel/sched/fair.c | **carried** =0011= |
| 18 | sched_ext idle SMT sibling | kernel/sched/ext_idle.c | **carried** =0012= |
| 16 | =free_contig_range()= bulk free | mm/page_alloc.c | **carried** =0021= |
| 17 | =vfree()= via =free_pages_bulk()= | mm/vmalloc.c | **carried** =0021= |

** Low-value / skip for rgx1gen11 (example)

| # | Reason to skip |
| 05 | Proportional newidle balance revert -- conflicts with BORE |
| 12 | Panther Lake C-states -- wrong CPU generation |
| 03-04, 10+ | Hardware-specific quirks (other vendors/boards) |
| 24-28 | i915 PSR/Panel Replay -- Panther Lake oriented; Raptor Lake Iris Xe may not benefit |

** Recommended weld order

1. Cherry-pick #06-#08 done (=0010=).
2. Add #09 (detach_tasks) done (=0011=).
3. Add #18 (sched_ext SMT idle) done (=0012=).
4. Add #16-#17 done (=0021=); memory-pressure benchmarks decide whether they
   remain in the default package (=linux-rg-mmhp=, blocked by =linux-rg-61pb=).

** Decision gate

Carry inline hunks (#06-#08) if they apply cleanly and =make -j9= kernel build
shows no sched regression in a desktop/build mixed trial. sched_ext idle (#18)
after first scx_loader A/B on hybrid CPU.

* Candidate: sched/fair detach_tasks loop_max fix

** Status

| Field | Value |
| Decision | carried (=0011-detach-tasks-fix.patch=) |
| Evidence grade | B (upstream-reviewed fix) |
| Source | cachyos-fixes-v27 patch 09/46; sirlucjan =7.0/cachyos-fixes-patches-v27= |
| vissue | =linux-rg-78xs= (DONE) |

** What it is

Fixes a fair-scheduler load-balance bug: =env.loop_max= in =detach_tasks()= was
set before acquiring the busiest runqueue lock, so the loop limit could disagree
with the actual =cfs.h_nr_queued= count under concurrent modification.

Patch behavior:

- Hold =env.loop_max= assignment until after =rq_lock_irqsave(busiest)=.
- Use =busiest->cfs.h_nr_queued= instead of =busiest->nr_running=.

Reviewed-by lines in the patch include Vincent Guittot and Valentin Schneider.

** Why it might help rgx1gen11

The i7-1365U is a hybrid 12-thread CPU. Load balancing across P-cores and
E-cores is frequent under mixed desktop and build workloads. Redundant
=detach_tasks()= scans waste CPU and can add latency jitter.

** Applicability: rgam5terra

| Profile | Status | Notes |
|---------|--------|-------|
| rgx1gen11 | carried | Hybrid load-balance motivation |
| rgam5terra | blocked (with 0010-0013 group) | Fix is vendor-neutral but bundled in hybrid weld; 9950X lacks P/E story |

If split from the hybrid weld in future, revisit as a standalone fair.c fix on
32-thread Zen 5 (grade B fix, weaker profile motivation).

** Interaction with BORE

BORE also modifies =kernel/sched/fair.c=. This fix targets =sched_balance_rq()=
/ =detach_tasks()=, a different region from BORE's burst scoring. Likely
compatible, but an Arch-line-offset adaptation patch may be required (same
pattern as =0002-bore-fair-arch-adapt.patch=).

** Evaluation plan

1. Apply patch to a test build with BORE enabled.
2. Under =make -j9= plus desktop load, sample scheduler stats if
   =CONFIG_SCHEDSTATS=y= (enabled in overlay).
3. Optional: SpecJBB or similar load-balance stress if available; original
   report cited redundant scans in long runs.

** Decision gate

Carry if the patch applies cleanly with BORE and does not break the fair.c
build. Low functional risk; main cost is rebase and adaptation maintenance.

* Candidate: proportional newidle balance revert (cachyos-fixes 05/46)

** Status

| Field | Value |
| Decision | not carried |
| Evidence grade | D (downstream policy choice) |

** What it is

Reverts mainline commit 33cf66d88306 ("sched/fair: Proportional newidle
balance"). Touches =kernel/sched/fair.c=, =topology.c=, =features.h=, and
related scheduler files.

** Why it is not carried

Large scheduler diff; conflicts with BORE in the same subsystem. CachyOS carries
it for desktop latency on hybrid CPUs, but linux-rg already commits to BORE plus
sched_ext. Prefer sched_ext experiments or this revert, not both plus BORE.

Revisit only if BORE is dropped.

* Memory patch welding map

The rgx1gen11 profile already enables mainline MGLRU, zswap, zram, PSI, and
DAMON. Three downstream memory directions overlap on =mm/vmscan.c=; a fourth
(cache-aware) is scheduler-side and independent of reclaim.

| Patch set | Primary files | Touch vmscan? | Weld partner |
| Cachy memory ratios | mm/Kconfig, mm/vmscan.c, Documentation/admin-guide/sysctl/vm.rst | yes | mglru-patches-v2 (best) |
| mglru-patches-v2 | mm/vmscan.c | yes | Cachy ratios (best) |
| re-swappiness v1.2 | mm_inline.h, mmzone.h, mm/vmscan.c, workingset.c | yes | conflicts with mglru-patches-v2 |
| cache-aware | fair.c, topology.c, sched.h, fork.c, init/Kconfig | no | none in mm; conflicts with BORE |

Recommended evaluation order for memory (issue =linux-rg-7f5e=):

1. Userspace only: PSI, oomd, zram/zswap policy (no vmscan conflict).
2. Extracted Cachy ratio knobs (anon/clean protection) without =CONFIG_CACHY=.
3. mglru-patches-v2 welded onto (2), carried as
   =0019-mglru-devtree-adapt.patch=.
4. re-swappiness only if (3) is rejected and as a single vmscan experiment.
5. cache-aware is a separate scheduler track (issue =linux-rg-kr7t=), not part
   of the memory weld.

Do not stack re-swappiness and mglru-patches-v2 without a manual unified patch;
both rewrite MGLRU reclaim control flow in =mm/vmscan.c=.

* Candidate: Cachy memory protection ratios (anon/clean min)

** Status

| Field | Value |
| Decision | carried as =0018-cachy-mm-ratios.patch= |
| Evidence grade | B (concept); D (downstream extract) |
| Source | CachyOS =6.17/0004-cachy.patch= (extract; do not carry full omnibus) |
| vissue | =linux-rg-7zwz= |

** What it is

Three sysctl knobs for reclaim protection on each NUMA node (rgx1gen11 is
effectively one node; rgam5terra will be evaluated as a single-socket Zen 5):

- =vm.anon_min_ratio= -- hard floor for anonymous pages (default 1%). Kconfig
  help explicitly mentions zram overfill thrash.
- =vm.clean_low_ratio= -- best-effort floor for clean file pages (default 15%).
- =vm.clean_min_ratio= -- hard floor for clean file pages (default 4%).

Extraction scope (ratio-only, no =CONFIG_CACHY=):

| File | Role |
| mm/Kconfig | =ANON_MIN_RATIO=, =CLEAN_LOW_RATIO=, =CLEAN_MIN_RATIO= defaults |
| mm/vmscan.c | vmscan-local sysctl table and reclaim/MGLRU gates |
| Documentation/admin-guide/sysctl/vm.rst | knob documentation |

Not required for ratios: Arch =CONFIG_CACHY=, Cachy fair.c tweaks, page-writeback
Cachy defaults, or swap cluster =#ifdef CONFIG_CACHY= blocks. Those stay out of
the extract.

** Why it fits rgx1gen11

The overlay enables zswap, zram, MGLRU, and PSI. Under browser plus build load,
anonymous reclaim into zram can thrash before PSI-driven userspace reacts. Ratio
knobs give a kernel-side floor aligned with SWAM and USENIX ATC 2020 framing
(see =docs/literature-review.org=).

** Weld with mglru-patches-v2

Complementary layers:

- Ratios: policy gates ("do not reclaim below X% on this node").
- mglru-patches-v2: MGLRU scan mechanics (evictable size, writeback handling,
  =try_to_inc_min_seq= refactor, batch eviction).

Both touch =mm/vmscan.c= but at different functions. A welded series should
apply mglru-patches-v2 first, then ratio hunks, then resolve any offset
conflicts in =shrink_lruvec= / =evict_folios= paths. Expect one Arch adapt
patch, not a blind stack.

** Weld with re-swappiness

Partial overlap on =mm/vmscan.c= and conceptually related (both tune MGLRU
reclaim). re-swappiness also changes =lrugen.max_seq[type]= per anon/file in
=mmzone.h= and =mm_inline.h= -- a direction mainline MGLRU may already mirror
in 7.0.12. Prefer mglru-patches-v2 plus ratios over re-swappiness plus ratios
unless re-swappiness alone wins a benchmark.

** Carried implementation

=0018-cachy-mm-ratios.patch= extracts the ratio mechanism without
=CONFIG_CACHY=, page-writeback defaults, swap-cluster defaults, or global
=sysctl.c= / =mm.h= extern wiring. Linux 7.0's VM sysctl table lives in
=mm/vmscan.c=, so the implementation keeps the four sysctls local to vmscan.

Compile check: =make -C /tmp/rg-linux-rg-mmcheck/linux-7.0.12 -j9 mm/vmscan.o=
after applying Arch 7.0.12 plus =0001= through =0019=.

Live pressure validation remains tracked by =linux-rg-7zwz= and
=linux-rg-5fxc=.

* Candidate: MGLRU dev-tree vmscan patch (mglru-patches-v2)

** Status

| Field | Value |
| Decision | carried as =0019-mglru-devtree-adapt.patch= |
| Evidence grade | B for MGLRU mainline; D for this dev-tree extract |
| Source | sirlucjan =7.0/mglru-patches-v2= |
| vissue | =linux-rg-5fxc= |

** What it is

Single-file =mm/vmscan.c= diff (~150 lines changed) merging MGLRU dev-tree
reclaim improvements. Notable changes:

- =handle_reclaim_writeback()= helper for flusher wakeup under memcg pressure.
- =lruvec_evictable_size()= for proportional protection sizing.
- =try_to_inc_min_seq()= signature and call-site refactor.
- =get_nr_to_scan()= / =evict_folios()= flow simplified with batch eviction and
  aging checks.

Upstream lineage: Yu Zhao MGLRU work (LPC 2024, kernel docs). This patch is a
downstream forward-port, not a separate published algorithm.

** Why it might help rgx1gen11

Improves MGLRU reclaim behavior under mixed file and anonymous pressure -- the
laptop's normal mode with browsers, builds, and NVMe cache.

** Weld matrix

| Partner | Verdict |
| Cachy memory ratios | Carried together in =0018= and =0019=; ratio gates preserved in =get_type_to_scan= |
| re-swappiness v1.2 | Do not stack; both rewrite the same vmscan functions |
| cache-aware | Independent (scheduler); no mm interaction |
| Mainline 7.0.12 only | Baseline retained through stock Arch kernel rollback |

** Carried implementation

=0019-mglru-devtree-adapt.patch= carries the dev-tree flow on top of =0018=.
One hunk is adapted because =0018= adds the =scan_control= argument to
=get_type_to_scan()= for anon/clean protection gates.

Compile check: =make -C /home/rgoswami/.local/share/chezmoi/.kernel-build/linux-rg-prepare-0018/linux-rg/src/linux-7.0.12 -j9 mm/vmscan.o=.

Live validation compares =0018= plus =0019= against stock Arch MGLRU with the
same zram/zswap userspace policy.

* Blocked fallback: re-swappiness v1.2

** Status

| Field | Value |
| Decision | blocked fallback (not carried); deprioritized vs mglru+ratios weld |
| Evidence grade | D (patch); A (problem framing) |
| Source | firelzrd / sirlucjan =7.0/re-swappiness-patches= |
| vissue | =linux-rg-phgx= |

** What it is

Re-swappiness v1.2 makes MGLRU anon and file generations age independently
(=lrugen.max_seq[type]=, per-type =lru_gen_is_active()=). Touches:

- =include/linux/mm_inline.h=
- =include/linux/mmzone.h=
- =mm/vmscan.c=
- =mm/workingset.c=

** Conflict with mglru-patches-v2

Both patches heavily modify =mm/vmscan.c= MGLRU eviction and aging. Git will
reject a naive sequential apply. Welding requires a manual unified patch
rebasing re-swappiness onto the mglru dev-tree layout -- high maintenance cost.

** Conflict with Cachy ratios

Ratios add reclaim gates in vmscan; re-swappiness changes scan sizing and
per-type seq logic in the same file. Weld is possible but harder than
mglru+ratios because of four files vs two.

** When to prefer re-swappiness

- mglru-patches-v2 does not apply to Arch 7.0.12.
- Benchmark shows per-type anon/file aging is the bottleneck and ratios alone
  are insufficient.
- Willing to maintain a custom four-file mm patch.

* Candidate: cache-aware scheduler (CONFIG_SCHED_CACHE)

** Status

| Field | Value |
| Decision | carried as =0020-cache-aware-sched.patch= |
| Evidence grade | D (downstream); B (hybrid CPU motivation) |
| Source | sirlucjan =7.0/cache-aware-patches= (=kernel-patches= repo) |
| vissue | =linux-rg-kr7t= |

** What it is

Not a memory patch. Adds =CONFIG_SCHED_CACHE= and scheduler logic to track a
task's preferred last-level cache (LLC) and align placement with cache topology:

| File | Role |
| include/linux/cacheinfo.h | cache hierarchy helpers |
| include/linux/sched.h | =sched_cache_time=, =preferred_llc=, =cache_work= |
| kernel/sched/fair.c | placement and migration consult preferred LLC |
| kernel/sched/topology.c | LLC-aware load balance hooks |
| kernel/fork.c | per-mm sched cache init |

** Why it might matter on i7-1365U

Raptor Lake U-series is hybrid with shared LLC across P and E clusters. Brown-Neri
LPC 2022 documents Linux hybrid scheduling gaps. cache-aware is a downstream
attempt to reduce cross-LLC migration; it is not the same as mainline Intel EAS
work.

** Why it might matter on rgam5terra (9950X)

Granite Ridge ships **two CCDs** with separate L3 caches. Cross-CCD migration is
the desktop analog of the laptop hybrid problem, but with homogeneous cores per
CCD. Kernel topology exposes NUMA/CCD boundaries via CPU topology sysfs; cache-aware
is a downstream attempt to reduce cross-cache migration. Evidence remains grade D
until bench on Terra hardware. **Higher priority** than on rgx1gen11 per
=docs/rgam5terra-kernel.org=.

** Applicability

| Profile | Status | Notes |
|---------|--------|-------|
| rgx1gen11 | carried | Hybrid LLC experiment |
| rgam5terra | evaluating (priority) | Dual-CCD locality; compare vs scx_loader |

** Carry constraints

- The patch applies on the BORE stack with offsets.
- It is enabled in =rgx1gen11.config= as =CONFIG_SCHED_CACHE=y=.
- Independent from the memory weld track; do not bundle with mglru or ratios.
- No peer-reviewed evaluation found (evidence grade D).
- Runtime value remains benchmark-gated against sched_ext userspace policies on
  1365U.

** Relationship to other scheduler candidates

| Candidate | Interaction |
| BORE | Same fair.c surface; compile-tested as a welded stack |
| sched_ext | Alternative path; literature grade B; already enabled in config |
| detach_tasks fix | Orthogonal small fair.c fix; compile-tested with =0020= |
| newidle revert | Also conflicts with BORE; not carried |

** Decision gate

Evaluate only on a branch without BORE, or after sched_ext experiments fail to
improve hybrid placement. Compare wakeup latency and build throughput against
BORE default and a sched_ext scheduler on the 1365U.

* Liquorix / Zen kernel (linux-lqx) -- what is worth taking?

** Status

| Field | Value |
| Decision | **partial cherry-picks** (0014-0015); no PDS / omnibus |
| Evidence grade | D (desktop laptop); B (concept for low-latency) |
| Source | damentz/liquorix-package 7.0-13, zen/v7.0.12-lqx1.patch |
| vissue | =linux-rg-lqx1= (omnibus cancelled); zen cuts tracked here |

** What Liquorix actually is (7.0.12-lqx1)

Liquorix is **not** a PREEMPT_RT kernel. It ships:

| Knob | Liquorix | Arch 7.0.12 (linux-rg base) |
| CONFIG_PREEMPT_RT | **off** | off |
| CONFIG_PREEMPT | voluntary (=y=) | voluntary (=y=) |
| CONFIG_PREEMPT_DYNAMIC | **off** (locked full preempt) | **on** (boot-tunable) |
| CONFIG_HZ | 1000 | 1000 (already) |
| CONFIG_SCHED_HRTICK | y | y (already) |
| Fair scheduler | **PDS** (=CONFIG_SCHED_PDS=y=) replaces EEVDF | EEVDF + BORE |
| Patch bulk | single ~130k-line zen omnibus | n/a |

Default CPU scheduler is **PDS** (Priority Deadline Skiplist), not BORE, not MUQSS,
not mainline EEVDF. Enabling PDS requires the full zen patch and conflicts with BORE,
0010-0013 fair.c work, and sched_ext coexistence testing.

** Arch 7.0.12 already contains most zen driver hunks

Applied the full ~130k-line zen omnibus on top of Arch 7.0.12 and diffed the
result. iwlwifi (mvm beacon rates, mld link/tx), i915 (PSR2 scanline WA, DP aux
parent-irq wait), and intel_pstate hybrid scaling all **reject as .rej** -- Arch
7.0.12 already ships the same fixes. No separate iwlwifi/i915 zen patches are
needed for AX211 / Iris Xe on rgx1gen11.

** Overlap with linux-rg already carried

| Liquorix idea | linux-rg equivalent |
| enter_lazy_tlb / finish_task_switch inlines | 0010 cachyos-fixes #06-08 (carried) |
| 1000 Hz tick | Arch base config (already) |
| Low-latency desktop goal | BORE + 0010-0013 + sched_ext (scx_lavd/bpfland) + ASA |
| iwlwifi AX211 beacon/AMSdu fixes | Arch 7.0.12 mainline (already) |
| i915 PSR/DP aux dock paths | Arch 7.0.12 mainline (already) |
| intel_pstate hybrid scaling | Arch 7.0.12 mainline (already) |
| =sysctl_sched_migration_cost= / ZEN_INTERACTIVE fair.c | 0013 (carried); skip zen fair.c |

** Zen cherry-picks carried (BORE-safe, boot-safe)

| Patch | Zen source | Why carry |
| 0014 | =mm/page_alloc.c= + =mm/vmscan.c= kswapd_waiters | Keeps kswapd awake while allocators or throttled direct reclaimers wait; reduces memory-pressure stalls under browser/build load |
| 0015 | =kernel/sched/cpufreq_schedutil.c= DL path | Sets =limits_changed= instead of =need_freq_update= when DL bandwidth exceeds minimum -- aligns with mainline schedutil limit plumbing on hybrid 1365U |

Both apply cleanly after 0001-0013; =page_alloc.o=, =vmscan.o=, =cpufreq_schedutil.o=
build on the linux-rg stack. No =CONFIG_ZEN_INTERACTIVE= or =CONFIG_SCHED_ALT=
required.

** Liquorix-only knobs (evaluated, not carried)

| Knob | Laptop fit | Boot risk | Verdict |
| CONFIG_RCU_NOCB_CPU_DEFAULT_ALL | Offloads RCU from all CPUs; can smooth jitter under load | Medium; power + hybrid interaction | **Hold**; try =preempt=full= first |
| CONFIG_RT_GROUP_SCHED=y | Only helps SCHED_FIFO/RR workloads | Low alone; needs PDS stack | Skip without PDS |
| PDS / BMQ alt scheduler | Desktop gaming benchmark narrative | **High**; replaces fair.c | **Do not** with BORE stack |
| CONFIG_ZEN_INTERACTIVE fair.c sysctl / elevator BFQ default | Desktop tuning bundled with PDS | Medium; conflicts 0013 + 0007 | **Skip** |
| zen i915 display hunks (PSR/DP) | ThinkPad panel + TB dock | n/a on Arch 7.0.12 | **Already upstream** |
| zen iwlwifi hunks | AX211 CNVi | n/a on Arch 7.0.12 | **Already upstream** |
| RCU_EXPERT tuning | Server/workstation | Medium | Skip on ultrabook |

** Boot-safe alternative without Liquorix rebuild

Arch already has =CONFIG_PREEMPT_DYNAMIC=y=. For a one-line latency experiment on
stock or linux-rg without recompiling:

#+begin_example
# cmdline (reversible): force full voluntary preemption
preempt=full
#+end_example

Compare against default dynamic preempt before touching RCU_NOCB or PDS.

** Decision gate

Port Liquorix only if BORE + sched_ext trials fail *and* you commit to maintaining
PDS + zen omnibus on every 7.0 bump. For rgx1gen11, prefer sched_ext userspace
(=linux-rg-tjsl=) over PDS.

* PREEMPT_RT (realtime kernel) -- what is worth taking?

** Status

| Field | Value |
| Decision | not carried |
| Evidence grade | A for control loops; **D** for rgx1gen11 daily driver |
| Source | linux-rt / =CONFIG_PREEMPT_RT= tree |
| vissue | =linux-rg-prert= |

** What it is

PREEMPT_RT converts most kernel spinlocks to sleeping locks and reduces non-preemptible
sections so SCHED_FIFO/RR deadlines are met. It is a **different kernel flavour**, not
a small patch on Arch PREEMPT.

Linux 7.x continues RT merge toward mainline, but Arch 7.0.12 desktop config does
**not** enable =CONFIG_PREEMPT_RT=.

** Why not on ThinkPad X1 Gen 11 daily driver

| Concern | Detail |
| Boot / driver coverage | iwlwifi, i915, SOF, NVMe paths have historically been RT lagging or fragile |
| Conflicts linux-rg stack | sched_ext + ASA + BORE tuned for !RT; rt-i915 (Cachy) needs RT and does not apply cleanly |
| Power / thermals | RT increases timer pressure; ultrabook idle power suffers |
| Use case mismatch | No hard real-time workload on this box (audio prod at most -> PipeWire + SCHED_FIFO userspace) |
| Boot safety | Highest regression risk of any option considered |

** What to use instead for "realtime feel"

| Need | Approach |
| Audio dropouts | PipeWire realtime module + user =@audio= group; not kernel RT |
| Low UI latency | BORE, 0010-0013, scx_lavd via scx_loader |
| Network jitter | BBR + fq; Wi-Fi power_save tuning (=linux-rg-iwl=) |
| Actual RT tasks | Separate RT VM or dual-boot linux-rt; never first linux-rg boot |

** Decision gate

Revisit PREEMPT_RT only for a dedicated benchmark partition or audio-prod experiment
with rollback, never as the primary rEFInd entry.

* Con Kolivas CK / MUQSS / BFS -- why not port?

** Status

| Field | Value |
| Decision | not carried; largely superseded |
| Last upstream release | 5.12-ck1 / MUQSS (April 2021); Kolivas retired CK/MUQSS Aug 2021 |
| Source | http://ck.kolivas.org/patches/ |

** What CK actually was

The CK patchset was a **full alternate scheduler class** (MUQSS: per-CPU
skiplist, earliest virtual deadline), not a set of fair.c tweaks. It replaced
CFS/fair scheduling wholesale on desktop-focused kernels (Arch =linux-ck=, etc.).
Goals: deterministic desktop latency, simple rules, no endless sysctl tuning.

MUQSS design explicitly evolved toward mainline EEVDF (Kolivas cites EEVDF/Staircase
Deadline lineage in =sched-MuQSS.txt=). Mainline 7.0 **is** EEVDF-based now.

** What linux-rg already takes from the Kolivas lineage

| Kolivas idea | linux-rg equivalent |
| MUQSS/BFS desktop interactivity | BORE burst scoring (fair-class tweak, not replacement) |
| SD scheduler =update_load_set= scaling | BORE patch cites Kolivas SD scheduler in =fair.c= comment |
| Smaller slices / lower migration cost | Partially mirrored in Cachy =CONFIG_CACHY= fair hunks -- not carried (BORE overlap) |
| Separate scheduler for low latency | =CONFIG_SCHED_CLASS_EXT=y= + scx_lavd / scx_bpfland / ASA router |

BORE is from Masahito Suzuki (firelzrd), not Kolivas, but the BORE fair.c patch
literally references Con Kolivas's SD scheduler for sysctl granularity logic.

** Why wholesale MUQSS port is a bad fit for 7.0.12 + rgx1gen11

1. **No maintained tree.** Last CK/MUQSS is 5.12 (2021). Forward-porting to 7.0
   is a multi-thousand-line scheduler rewrite with no upstream reviewer.
2. **Architectural conflict.** MUQSS adds =SCHED_MUQSS= and bypasses EEVDF fair
   class. linux-rg commits to EEVDF + BORE + sched_ext + ASA. You cannot stack
   MUQSS on top; you must *replace* the fair stack and drop BORE.
3. **Hybrid CPUs.** i7-1365U P/E topology post-dates MUQSS retirement. Kolivas
   design predates Intel hybrid load-balance work; mainline + scx experiments are
   the current answer for P/E placement.
4. **Kolivas's own conclusion.** Aug 2021 blog: ending MUQSS/CK maintenance;
   mainline had absorbed key ideas; further desktop work belongs in different
   approaches (community moved to gaming patches, scx schedulers).
5. **Better modern equivalents.** For MUQSS-like "desktop feel without ripping
   out EEVDF": try =scx_bpfland= or =scx_lavd= via scx_loader (sched_ext), or
   benchmark BORE (already carried) before attempting a dead scheduler port.

** If you still want Kolivas-style behaviour

| Approach | Cost | Recommendation |
| Port 5.12 MUQSS to 7.0 | Very high; unmaintained | Do not |
| Replace BORE with MUQSS branch kernel | Loses BORE, sched_ext coexistence testing | Only as separate experimental branch |
| Extract non-scheduler CK hunks from 5.12 | High rebase; likely upstream already has them | Only if a specific hunk is identified |
| scx_bpfland / scx_lavd on hybrid CPU | Userspace; reversible | **Try first** |
| cachyos-fixes #06-#08 inline hot paths | Low; compatible with BORE | Carried as =0010= |

** Decision gate

Revisit MUQSS only if BORE + sched_ext experiments fail on repeated hybrid-CPU
desktop trials *and* someone commits to maintaining a 5.12->7.0 scheduler forward
port. Otherwise treat Kolivas work as historical design input already partially
present in BORE and mainline EEVDF.

* CachyOS kernel patchset (CK / build / rust) -- deep dive

** Clarification: "CK" in CachyOS context

CachyOS does not ship Con Kolivas's historical CK/MUQSS patchset on 7.0.
The omnibus =0004-cachy.patch= (6.17) and sirlucjan 7.0 splits use
=CONFIG_CACHY= as a compile-time gate for Cachy-specific fair-class and
misc tweaks, not MUQSS. BORE in linux-rg already carries burst scoring
from the same author lineage and overlaps the fair.c region.

| Layer | Source (7.0) | Primary files | rgx1gen11 verdict |
| CONFIG_CACHY fair tweaks | Cachy 6.17 omnibus | kernel/sched/fair.c, sched.h | not carried; conflicts with BORE |
| Compiler ISA (x86-64-v3, znver4) | cpu-cachyos-patches | arch/x86/Kconfig.cpu, Makefile | not carried on rgx1gen11 (Intel); rgam5terra evaluates znver5 separately |
| -O3 kernel build | kbuild-cachyos-patches | Makefile, init/Kconfig | not carried; no laptop win shown over Arch =-O2= |
| ThinLTO distributed backend | kbuild-dev-patches | Makefile, scripts/Makefile.thinlto, vmlinux_a | not carried while remote builders are unavailable |
| Rust KBUILD_RUSTFLAGS sync | omnibus + cpu patch | Makefile, arch/x86/Makefile | mirrors CFLAGS (-Copt-level, -Ctarget-cpu) |

** CONFIG_CACHY fair-class section (omnibus excerpt)

When =CONFIG_CACHY=y=, Cachy lowers EEVDF base slice and migration cost:

- =sysctl_sched_base_slice=: 350000 ns (vs 700000 mainline in that extract).
- =sysctl_sched_migration_cost=: 300000 ns (vs 500000).
- =SCHED_NR_MIGRATE_BREAK=: 8 (same threshold as PREEMPT_RT path).

These are desktop-latency policy choices without published evaluation (grade D).
linux-rg keeps BORE instead; do not enable =CONFIG_CACHY= fair hunks on this
profile.

** Why Rust is in the kernel (not a perf play)

Kernel Rust is **not** pushed because Rust is faster than C. The goal is:

| Goal | Mechanism |
| Memory safety | Type system + borrow checker eliminate large classes of UAF, double-free, and data races in *new* code |
| Safer driver authoring | =rust/kernel/= abstractions wrap C APIs; leaf drivers use safe APIs where possible |
| Incremental migration | New subsystems in Rust (binder IPC, Nova/Tyr DRM, rnull block, ...) without rewriting all of C |
| Security ROI | Industry consensus (Android, Google, Microsoft research) that kernel CVE reduction justifies the investment |

References: kernel =Documentation/rust/general-information.rst=; Rust-for-Linux
project; Greg K-H on permanent Rust support (2025); USENIX ATC 2024 Android
binder Rust evaluation.

On Arch 7.0.12 with =CONFIG_RUST=y=, the **active** Rust path on a desktop
build is mainly =CONFIG_ANDROID_BINDER_IPC_RUST=y= (binder IPC). Iris Xe uses
=CONFIG_DRM_I915= (C), not =CONFIG_DRM_NOVA= (Rust). So enabling Rust does
**not** rewrite the graphics, Wi-Fi, or NVMe stack on rgx1gen11 today.

** Cachy =KBUILD_RUSTFLAGS= is a separate, smaller question**

CachyOS syncs =KBUILD_RUSTFLAGS= with =KBUILD_CFLAGS= (-Copt-level, -Ctarget-cpu).
That only affects the **small fraction** of the image compiled from =*.rs=
(binder, optional Nova/rnull modules). It is build tuning for Rust objects, not
the reason upstream adopted Rust.

1. =CC_OPTIMIZE_FOR_PERFORMANCE_O3=: sets =KBUILD_RUSTFLAGS += -Copt-level=3=
   alongside =-O3= for C -- requires =kbuild-cachyos-patches= Kconfig symbol.
2. CPU target flags: =KBUILD_RUSTFLAGS += -Ctarget-cpu=x86-64-v3= tracks
   =-march=*= (cpu-cachyos-patches).
3. ThinLTO dist mode (kbuild-dev-patches): distributed LTO backend split.

** rgx1gen11 Rust pins (carried in overlay)**

Arch 7.0.12 already enables Rust. =rgx1gen11.config= pins:

| Symbol | Role on 21HM002GUS |
| =CONFIG_RUST=y= | Build Rust support (binder, fw_loader abstractions) |
| =CONFIG_ANDROID_BINDER_IPC_RUST=y= | Rust binder IPC (Waydroid / Android container path) |
| =CONFIG_RUST_FW_LOADER_ABSTRACTIONS=y= | Safer firmware-loader API used by iwlwifi/SOF paths |

These do **not** replace iwlwifi, i915, or nvme C drivers. Benefit is compile-time
safety on firmware load and optional Android IPC; not a latency patch.

** Rust drivers in Linux 7.0.12 (hardware relevance)

| Driver / module | Kconfig | rgx1gen11 verdict |
| Binder IPC (Rust) | =ANDROID_BINDER_IPC_RUST= | **Pinned** (Arch default; Waydroid-ready) |
| Firmware loader abstractions | =RUST_FW_LOADER_ABSTRACTIONS= | **Pinned** (iwlwifi/SOF firmware) |
| Nova / Tyr DRM | =DRM_NOVA=, =DRM_TYR= | **Skip** (NVIDIA discrete GPUs only) |
| rnull block | =BLK_DEV_RUST_NULL= | **Skip** (test driver) |
| cpufreq DT Rust | =CPUFREQ_DT_RUST= | **Skip** (ARM device-tree cpufreq) |
| DRM panic QR | built with =DRM_PANIC= | **Optional** debug; no perf win |
| HID Rust | none in production 7.0 | ELAN/SYNA use C =i2c_hid= |

** What not to expect from Rust on rgx1gen11**

- No Rust rewrite of Iris Xe (=i915=), AX211 (=iwlwifi=), or KIOXIA (=nvme=) in 7.0.
- =KBUILD_RUSTFLAGS -O3 / -Ctarget-cpu=x86-64-v3= only affects the small =*.rs=
  object set (binder); measure compile time before carrying Cachy kbuild patches.
- scx_rusty is a **sched_ext BPF scheduler name**, not kernel Rust code.

** Overlay hygiene**

Do not add =CONFIG_ZEN_INTERACTIVE= or =CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE_O3=
without the matching Cachy Kconfig patches; symbols would be inert or fail
=olddefconfig=.

** Decision gate (kbuild / rust / CONFIG_CACHY)

| Patch | Carry? | Blocker |
| CONFIG_CACHY fair | no | BORE stack |
| cpu znver4 | no on rgx1gen11 | wrong CPU vendor |
| cpu znver5 / x86-64-v4 | rgam5terra only | evaluate after PPT thermal gate |
| x86-64-v3 ISA choice | no default | needs benchmark vs Arch generic |
| -O3 / rust opt-level 3 | no default | compile-time and size study |
| ThinLTO dist | optional | dist builder infra only |

* Carried: ASA sched_ext router (arXiv:2511.11628 prototype)

** Status

| Field | Value |
| Decision | carried (prototype) |
| Evidence grade | D (research paper; heuristic kernel port) |
| Patch | =0009-asa-sched-router.patch= |
| Config | =CONFIG_SCHED_ASA=y= (depends on =SCHED_CLASS_EXT= + =PSI=) |
| Userspace | =asa-router= -> =/usr/bin/asa-router= |

** What it is

Mixture-of-Schedulers (MoS) proposes an Adaptive Scheduling Agent (ASA) that
routes workloads to expert sched_ext policies instead of designing one monolithic
fair-class scheduler. The paper (Wang et al., arXiv:2511.11628) uses offline ML
(XGBoost ensemble) plus a three-stage calibration pipeline; runtime action loads
expert BPF schedulers via sched_ext.

linux-rg implements a kernel prototype faithful to the architecture split:

| Module | Implementation |
| Perception | PSI averages, runqueue load, context-switch rate -> sysfs |
| Decision | Heuristic classifier + time-weighted probability voting |
| Action | sysfs =recommended_scheduler= + uevents; =asa-router= calls =scx_loader= |

Default expert mapping (paper portfolio subset):

| Workload class | Default scx expert |
| interactive | scx_lavd |
| cpu | scx_rusty |
| io | scx_bpfland |
| mixed | scx_p2dq |

** Sysfs interface

=/sys/kernel/sched_ext/asa/=:

- =perception= -- sampled metrics (read-only).
- =workload= -- voted class name.
- =recommended_scheduler= -- mapped expert for current class.
- =mapping= -- =class=scheduler= table (read/write).
- =enabled= -- start/stop sampling timer.
- =cooldown_ms=, =sample_ms=, =voting_window=, =decay_pct= -- tunables.

** How to test

#+begin_src sh
# after booting linux-rg with CONFIG_SCHED_ASA
cat /sys/kernel/sched_ext/asa/mapping
echo 1 | sudo tee /sys/kernel/sched_ext/asa/enabled
watch -n1 cat /sys/kernel/sched_ext/asa/recommended_scheduler

# userspace action (needs scx_loader + scx-scheds)
sudo asa-router --enable --dry-run   # observe recommendations
sudo asa-router --enable             # live switching
#+end_src

** Limitations

- No XGBoost model shipped; heuristic classifier only until offline training
  pipeline from the paper is reproduced.
- Scheduler switch still requires userspace BPF load (sched_ext design).
- Not a replacement for BORE; operates at sched_ext layer when enabled.

* Userspace policy candidates (not kernel patches)

** systemd-oomd plus PSI

| Field | Value |
| Decision | userspace candidate |
| Evidence grade | B/C |

PSI is enabled in the kernel. Meta's OOMD work and systemd memory pressure
documentation support userspace reaction to =/proc/pressure/memory= without a
kernel patch. See =docs/literature-review.org=.

** sched_ext scheduler selection (scx_loader)

| Field | Value |
| Decision | userspace |
| Evidence grade | B |

Kernel has =CONFIG_SCHED_CLASS_EXT=y=. Literature and LPC 2024 materials support
trying scx schedulers on hybrid CPUs before adding more fair-class patches.

* Kernel patch ecosystem survey (wide)

Survey of well-known third-party kernel traditions, evaluated against Arch 7.0.12
and the BORE + sched_ext stack on 21HM002GUS. Live runtime probe 2026-06-18.

** Scheduler replacement stacks (do not port wholesale)

| Ecosystem | Core idea | vs linux-rg |
| Liquorix / Zen | PDS replaces EEVDF; ZEN_INTERACTIVE | Omnibus cancelled; 0014-0015 cherry-picks only |
| XanMod | MQ/deadline fair tweaks; PREEMPT variants | Conflicts with BORE; cherry-pick MM/driver only |
| linux-ck / Kolivas MUQSS | Alternate fair class (skiplist EVD) | Dead since 5.12; use BORE + scx_* instead |
| PREEMPT_RT / linux-rt | RT spinlock conversion | Cancelled for daily driver (boot risk) |
| linux-tkg | Fsync (mainline now), zenify, optional PDS | High rebase; low laptop value |

** Compatible tweak pools (linux-rg source mines)

| Ecosystem | Taken from it | Status |
| CachyOS / sirlucjan | BORE, 0010-0013, block 0007, ADIOS 0008, ratios/mglru carries | Primary pool |
| firelzrd | BORE, ADIOS, re-swappiness | BORE + ADIOS carried |
| pf-kernel | BBRv3, v4l2loopback | 0003-0005 carried |
| Liquorix zen | kswapd_waiters, schedutil limits, cpuidle TEO | 0014-0016 carried |
| sched_ext userspace | scx_lavd, scx_bpfland, scx_rusty, ASA | Userspace eval track |

** Build / ISA tuning (optional)

| Ecosystem | Knobs | rgx1gen11 verdict |
| Cachy kbuild | -O3, KBUILD_RUSTFLAGS, ThinLTO dist | Candidate for dist builds only |
| Clear Linux | AutoFDO, aggressive -march | Project ended; skip |
| x86-64-v3 / znver4 ISA | cpu-cachyos-patches | Intel 1365U: skip znver4; v3 optional bench |

** Security / specialty trees (skip on this box)

| Ecosystem | Why skip |
| linux-hardened | Personal dev laptop threat model |
| AUFS / UKSM | Unmaintained on 7.0 |
| Handheld / Deck patches | Wrong form factor |
| cgroup VRAM / rt-i915 | iGPU laptop; no RT |

** Live hardware probe (2026-06-18) -- changes priority

| Signal | Observed | Action |
| cpuidle governor | **menu** (not TEO) | Carry 0016; gate with linux-rg-bench-idle |
| intel_pstate | **performance** on all CPUs | userspace linux-rg-pwr (before more kernel patches) |
| iwlwifi power_save | **Y** globally | per-profile tuning linux-rg-iwl |
| zram | **~15.5 GiB** on 32 GiB RAM | resize policy linux-rg-zram (8-16 GiB target) |
| NVMe scheduler | **bfq** active | ADIOS/none A/B linux-rg-rt1t |
| Platform profile | balanced | linux-rg-pwr A/B vs low-power/performance |
| Zen iwlwifi/i915 | .rej on Arch 7.0.12 | skip; no driver zen patches needed |

** Hardware-specific priority queue (post-survey)

1. Boot safely (linux-rg-mxx8, linux-rg-boot)
2. Userspace power: pstate governor, platform_profile, iwl power_save (linux-rg-pwr, linux-rg-iwl)
3. Kernel carries: 0014-0016 applied; cpuidle TEO validated on bench-idle
4. zram + oomd (linux-rg-zram, linux-rg-oomd)
5. scx_loader + ASA (linux-rg-tjsl)
6. NVMe scheduler A/B (linux-rg-rt1t)
7. Memory vmscan weld if PSI tests fail (linux-rg-7zwz, linux-rg-5fxc)
8. cachyos-fixes #16-17 carried as =0021=; memory-pressure bench decides default retention (linux-rg-mmhp)

Vissues: =linux-rg-eco1= (this survey), =linux-rg-zen2= (0014-0015), =linux-rg-rust1= (Rust pins),
=linux-rg-7m20= (0016 + bench-idle gate).

* Candidate matrix (extended)

| Area | Candidate | Source | Decision | Evidence | Notes |
| Block | ADIOS | CachyOS 6.17 omnibus | carried (module) | D | Non-default; see ADIOS section above |
| Memory | Cachy anon/clean ratios | Cachy 6.17 omnibus extract | carried =0018= | B/D | Extracted without =CONFIG_CACHY=; welded with mglru-patches-v2 in =0019= |
| Memory | mglru-patches-v2 | sirlucjan 7.0 | carried =0019= | D | Adapted after ratio extract; not stacked with re-swappiness |
| Memory | re-swappiness v1.2 | firelzrd / sirlucjan 7.0 | blocked fallback | D / A framing | Try only if mglru+ratios weld fails |
| Scheduler | hot-path inlines | cachyos-fixes #06-08 / 0010 | carried | B/D | Hybrid laptop context-switch path |
| Scheduler | detach_tasks fix | cachyos-fixes #09 / 0011 | carried | B | 1365U load balance |
| Scheduler | sched_ext SMT idle | cachyos-fixes #18 / 0012 | carried | B | 2-way SMT wake |
| Scheduler | migration_cost 300us | Cachy extract / 0013 | carried | D | Kolivas-adjacent sysctl |
| Scheduler | cache-aware (SCHED_CACHE) | sirlucjan 7.0 | carried =0020= | D | Enabled for LLC locality A/B against sched_ext |
| Scheduler | ASA router (MoS) | arXiv:2511.11628 / 0009 | carried (prototype) | D | sched_ext layer; needs scx_loader |
| Build | Cachy -O3 / rust opt-level 3 | kbuild-cachyos-patches | not carried | D | compile tuning only; no local win shown |
| Build | ThinLTO dist / vmlinux_a | kbuild-dev-patches | not carried | D | remote-builder infra unavailable |
| Build | x86-64-v3 / CONFIG_CACHY fair | cpu-cachyos / omnibus | not carried | D | BORE replaces CACHY fair |
| MM | kswapd_waiters | Liquorix zen / 0014 | carried | D | Memory-pressure responsiveness |
| CPUfreq | schedutil DL limits | Liquorix zen / 0015 | carried | D | Hybrid 1365U |
| Power | cpuidle TEO default | sirlucjan 7.0 / 0016 | carried | B | Live probe: menu governor today |
| Rust | binder + fw_loader pins | Arch 7.0.12 mainline | carried (config) | B/C | Not a driver rewrite |
| Scheduler | newidle balance revert | cachyos-fixes-v27 #05 | not carried | D | Conflicts with BORE |
| Memory | vmalloc/page bulk free #16-17 | cachyos-fixes-v27 / 0021 | carried | B/D | Needs memory-pressure bench |
| Policy | systemd-oomd | Meta PSI/OOMD | userspace | B/C | Pairs with CONFIG_PSI |
| Policy | scx_loader | sched_ext ecosystem | userspace | B | Hybrid CPU experiments |

* Wide net appendix (userspace, platform, distro mines)

Ongoing survey (=linux-rg-49gh=). Kernel patch rivers are in the ecosystem survey
above; this section tracks **everything else** tweakable for 21HM002GUS: thermals,
firmware, storage path, BPF tooling, distro hardware quirks, and chezmoi assets
already on =rgx1gen11= but not yet tied to linux-rg measurement.

** Survey method

1. Web search + Arch/kernel/PipeWire/sched_ext docs.
2. Grade A-D same as =docs/literature-review.org=.
3. Add vissue child under =linux-rg-49gh= when a lane needs implementation.
4. Promote to carried/config only after =linux-rg-59jn= bench matrix or boot smoke.

** Thermals, fans, and power platform (=linux-rg-m00h=)

| Candidate | Source | Layer | Grade | rgx1gen11 notes |
| intel_dptf driver | https://docs.kernel.org/driver-api/thermal/intel_dptf.html | kernel (pinned) | B | =CONFIG_ACPI_DPTF=y= in overlay; policy/userspace thin |
| ACPI platform_profile | mainline + ThinkPad ACPI | kernel + sysfs | B | Live =balanced=; =rg-x1gen11-profile= switches per workload |
| TLP | chezmoi =run_onchange_linux_laptop_power= | userspace | B | Charge 75/80, USB autosuspend; **disables power-profiles-daemon** |
| power-profiles-daemon | Fedora/Arch default | userspace | C | Conflicts with TLP; A/B only if TLP disabled on a test partition |
| thermald | https://wiki.archlinux.org/title/Fan_speed_control | userspace | C | May overlap DPTF; test fan spin under build-heavy before enabling |
| thinkfan | ibm/acpi fan | userspace | C | Only if =/proc/acpi/ibm/fan= present on 21HM002GUS |
| intel_pstate HWP / EPP | kernel docs | sysfs | B | Live probe: =performance= on all CPUs; profile should use =schedutil= daily |
| x86_energy_perf_bias | msr-tools / kernel | sysfs | C | Pair with travel vs build-heavy profiles |
| intel_pmc_core / PMT | =CONFIG_INTEL_PMC_CORE=m=, PMT telem | kernel module | B | Telemetry for idle power forensics (=linux-rg-sf6i=) |
| PCIe ASPM supersave | =CONFIG_PCIEASPM_POWER_SUPERSAVE=y= | kernel | B | Already pinned; validate idle with =linux-rg-bench-idle= |
| rg-caffeine | chezmoi | userspace | C | TLP performance/AC mode for focused work; not a kernel governor |
| Kernel cmdline cstates | =intel_idle.max_cstate=, etc. | boot param | C | Last resort after TEO + TLP bench |

mjg59 DPTF/platform_profile write-up (2021): ACPI profile attribute on thermal
zones -- useful context for why userspace must not fight firmware DTT.

** Observability and BPF (=linux-rg-sf6i=)

| Candidate | Source | Use on 1365U | Grade |
| PSI =/proc/pressure/*= | kernel + Meta docs | oomd, =rg-psi-snapshot=, bench matrix | A |
| bpftrace | https://opensource.com/article/19/8/introduction-bpftrace | PSI, sched wake, block latency scripts | B |
| perf sched / stat | mainline | Hybrid migration, BORE vs scx A/B | B |
| ftrace / function_graph | =CONFIG_FTRACE=y= pinned | Scheduler stall debug | B |
| BTF / CO-RE BPF | =CONFIG_DEBUG_INFO_BTF=y= pinned | sched_ext, bpftrace portability | B |
| intel_pmt / pmtctl | Intel PMT spec, Telegraf plugin | Package power, uncore readouts | C |
| schedstats | =CONFIG_SCHEDSTATS=y= | /proc/schedstat post-boot analysis | C |
| powertop | userspace | Idle benchmark (=linux-rg-bench-idle=) | B |

Red Hat OpenShift PSI article (2026): production framing for memory pressure --
same signal as Meta oomd and systemd-oomd on this box.

** Storage path: LUKS, btrfs, NVMe, swap (=linux-rg-2r1r=)

| Candidate | Source | Layer | Grade | Notes |
| btrfs compress=zstd | btrfs.readthedocs.io | mount | B | Chezmoi host uses btrfs; profile-specific mount opts candidate |
| btrfs noatime / autodefrag | btrfs docs | mount | C | Measure on XG8 before autodefrag on SSD |
| LUKS overhead | Arch/dm-crypt guides | setup | B | Already in production layout; document sequential read impact |
| btrbk + rg-space-sweep | chezmoi | userspace | B | Snapshot lifecycle; IO during balance affects build bench |
| BFQ udev default | chezmoi laptop power | udev | B | Forces bfq on nvme*; pairs with ADIOS module eval |
| ADIOS / none / mq-deadline | HOTSTORAGE 2023 + =linux-rg-rt1t= | kernel module | D | ADIOS carried; not default |
| zram writeback | =CONFIG_ZRAM_WRITEBACK=y= | kernel + policy | C | Enabled in config; no writeback target policy yet |
| zswap pool tuning | =rg-zswap-tune= stub | sysfs | C | Secondary to zram sizing (=linux-rg-zram=) |
| zram vs zswap stack | chrisdown 2026, LF zram slides | policy | B | Literature framing; local sizing still unknown |

** Network edge and Wi-Fi (=linux-rg-ervg=, =linux-rg-iwl=)

| Candidate | Source | Grade | Notes |
| BBR + fq default | carried 0003-0004 | A | Daily driver; Cubic fallback for captive portals |
| Cubic / Reno modules | mainline | A | =modprobe tcp_cubic= for travel A/B |
| CAKE qdisc | bufferbloat.net | C | Egress shaping on hotel Wi-Fi |
| fq_codel | mainline | C | Alternative qdisc for bufferbloat |
| TCP_NOTSENT_LOWAT | sysctl | C | Interactive feel on VPN |
| iwlwifi power_save | module param + =iw= | B | =rg-x1gen11-profile=; travel vs docked |
| iwlwifi bt_coex_active | =rgx1gen11-iwlwifi.conf= | modprobe | B | CNVi + USB BT; already shipped |
| iwd roam | chezmoi (iwd not NM) | userspace | B | =linux-rg-bench-wifi= |

** Audio and low-latency feel (=linux-rg-mvxn=)

| Candidate | Source | Grade | Notes |
| PipeWire module-rt | https://docs.pipewire.org/page_module_rt.html | B | Preferred over PREEMPT_RT daily driver |
| @audio realtime group | Arch wiki | B | ulimits + PipeWire; pair with SOF stack |
| SCHED_FIFO for pipewire | limits.conf | C | Watch =sched_rt_runtime_us= starvation |
| SOF + HDA path | =linux-rg-sof= | kernel | B | Config pinned; resume testing =linux-rg-slp= |

** Process-level scheduling (=linux-rg-db49=)

| Candidate | Source | Grade | Notes |
| ananicy-cpp | CachyOS forum + sched_ext threads | D | Per-app nice/IO; after scx ladder pick |
| cgroup slices | chezmoi browser/build/rust-analyzer | B | Already deployed |
| systemd CPUWeight/IOWeight | slice units | B | Profile-tuned via =rg-x1gen11-profile= |
| scx_loader + sched ladder | chezmoi | B | 14 schedulers; bench =linux-rg-tjsl= |
| auto-cpufreq | PyPI | D | Redundant with TLP + intel_pstate; skip unless TLP fails |

** Firmware and platform (=linux-rg-wc69=)

| Candidate | Source | Grade | Notes |
| fwupd / LVFS | Lenovo | B | BIOS 1.39 baseline; sleep/Wi-Fi fixes often here first |
| linux-firmware iwl/SOF | Arch package | A | Required for boot; version pin after linux-rg boot |
| ThinkPad ACPI hotkeys | =CONFIG_THINKPAD_ACPI=m= | kernel | B | Fn lock, rfkill, charge control via TLP |

** Distro / hardware reference mines (not ports)

| Mine | Use | Port? |
| Arch wiki X1 Carbon Gen 11 | Hardware quirks, suspend | Reference only |
| NixOS nixos-hardware ThinkPad | Module param ideas | Cherry-pick ideas |
| Gentoo power management guide | MSR, cpufreq trivia | Reference |
| CachyOS wiki sched_ext | scx_loader UX | Align with =rg-sched-ladder= |
| Framework/Meteor Lake power blogs | DTT/idle methodology | Transfer carefully to Raptor Lake-U |
| sched-ext.com / LPC 2026 plans | Scheduler roadmap | Informs =linux-rg-tjsl= |

** Chezmoi assets already on host (integrate with linux-rg)

| Asset | Location | Linked vissue |
| TLP + BFQ udev + nftables | =.chezmoiscripts/run_onchange_linux_laptop_power= | =linux-rg-m00h=, =linux-rg-chzm= |
| Memory tuning sysctl/zram/oomd | =run_onchange_linux_memory_tuning= | =linux-rg-7f5e= |
| Industry stack scripts | =dot_local/bin/rg-*= | =linux-rg-chzm= |
| rg-space-sweep / btrfs | =dot_local/share/rg-space-sweep-src= | =linux-rg-2r1r= |
| rg-caffeine | chezmoi systemd user units | =linux-rg-pwr= |
| xe/i915 modprobe | laptop power script | =linux-rg-i915= |

** Wide net pass 2 (2026-06-18 web search)

| Candidate | Source | Layer | Grade | rgx1gen11 action |
| Intel HFI (Hardware Feedback Interface) | https://docs.kernel.org/arch/x86/intel-hfi.html | kernel sysfs | B | Check =/sys/devices/system/cpu/cpu*/cpu_capacity=; complements Thread Director on hybrid 1365U (=linux-rg-tjsl=); no patch needed |
| Thread Director visibility | Phoronix HFI enable/disable; intel_hfi | firmware+kernel | B | OS hint layer for P/E placement; bench with =detach_tasks= + scx ladder (=linux-rg-tjsl=) |
| NVMe APST / autopm | Arch NVMe wiki; nvme-cli | sysfs/module | B | =nvme id-ctrl= / =nvme get-feature= on XG8; document latency vs battery for =linux-rg-2r1r= |
| nvme noapic / I/O errata | linux-nvme list (Kioxia) | reference | C | Watch dmesg on linux-rg boot; no preemptive patch |
| TB4 dock HDR/DDCCI gaps | Fedora discussion (TB3/TB4 onboard DP) | platform | C | Docked-office profile: test external brightness; kernel =CONFIG_DDCCI=m= carried (0006) |
| ddcutil / ddcui | https://www.ddcutil.com/ | userspace | B | Pair with in-tree DDCCI; vissue =linux-rg-ddcci= |
| thermald on modern Intel | Endeavour forum; intel/thermal_daemon | userspace | C | Often redundant with DPTF on Raptor Lake; enable only if fan/thermal gaps after =linux-rg-m00h= baseline |
| TLP vs thermald | TLP docs (linrunner.de) | policy | B | TLP already primary; thermald is optional second daemon -- avoid both without measurement (=linux-rg-m00h=) |
| KIOXIA XG8 ULL class | Tom's Hardware review | hardware | C | Informs ADIOS question (=linux-rg-rt1t=); not a license to set ADIOS default without fio |

** Live hardware validation log (=linux-rg-e9fx=, 2026-06-18)

These probes ran on =rgx1gen11= (21HM002GUS) against the **stock** Arch kernel
=7.0.12-arch1-1=. They validate *platform facts* and *userspace stack gaps*; they
do **not** validate linux-rg patches (BORE, BBR, TEO default, sched_ext, ADIOS)
until =linux-rg-mxx8= first boot.

| Check | Command / path | Observed | Verdict |
| Kernel identity | =uname -r= | =7.0.12-arch1-1= (stock) | Baseline only; linux-rg unbooted |
| TCP CC | =sysctl net.ipv4.tcp_congestion_control= | =cubic= (not BBR) | Patch 0003 **not active** on running kernel |
| Default qdisc | =sysctl net.core.default_qdisc= | =fq_codel= | BBR+fq carry **not active** |
| cpuidle governor | =/sys/.../cpuidle/current_governor= | =menu= (TEO available) | 0016 TEO default **not active** |
| Hybrid topology | =lscpu -e= MAXMHZ | CPUs 0-3: 5200 MHz; 4-11: 3900 MHz | P/E split **confirmed** |
| HFI sysfs | =/sys/devices/system/cpu/intel_hfi= | absent; all =cpu_capacity=1024 | =CONFIG_INTEL_HFI_THERMAL=y= only; capacity hints **not exposed** |
| sched_ext | =/sys/kernel/sched_ext/state= | =disabled= (stock has class, no BPF loaded) | sysfs **present** on 7.0.12; bench needs scx_loader attach |
| ADIOS module | =modinfo adios= | not present | Carried patch **untested** |
| NVMe model | =nvme list= | KIOXIA XG8AZNV1T02 LA, FW 5108APLA | Hardware ID **confirmed** |
| NVMe scheduler | =/sys/block/nvme0n1/queue/scheduler= | =[bfq]= | Chezmoi udev policy **active** |
| APST | =nvme get-feature -f 0x0c= | needs root; not captured | Lane =linux-rg-8s4g= blocked without sudo bench |
| zram + swap | =/proc/swaps= | zram ~15.5 GiB + btrfs swapfile ~14.9 GiB | Chezmoi memory tuning **active** |
| swappiness | =/proc/sys/vm/swappiness= | 100 | Matches chezmoi template |
| MGLRU | =CONFIG_LRU_GEN=y=, =min_ttl_ms=0= | enabled | Mainline pin **confirmed** |
| damon_reclaim | =/sys/module/damon_reclaim= | module not loaded | Config built-in (=y=) but **not exercised** until =rg-damon-reclaim= |
| PSI memory | =/proc/pressure/memory= | some avg10 low at idle probe | PSI **readable** |
| PSI CPU | =rg-psi-snapshot once= | cpu some avg10 ~44% during probe | High CPU pressure under background load; use **fixed-idle** bench |
| systemd-oomd | =systemctl is-active= | active; ManagedOOMMemoryPressure=auto | Chezmoi layer **active** |
| Meta oomd | =systemctl is-active oomd= | inactive | Optional layer not installed |
| iwlwifi | =power_save=Y=, =bt_coex_active=Y= | both on | Modprobe defaults **active** |
| irqbalance | =systemctl is-active irqbalance= | inactive | Already disabled; =linux-rg-kqlk= **pre-satisfied** |
| thinkfan path | =/proc/acpi/ibm/fan= | =status:enabled speed:6325= | Manual fan control **possible** (=linux-rg-m00h=) |
| TB4 | =/sys/bus/thunderbolt/devices/*= | domains present, authorized=1 | Dock path **reachable**; =boltctl= not installed |
| Profile stack | =rg-x1gen11-profile doctor= | no active profile; governor =performance= | Industry scripts were **not on PATH** until symlink fix; apply profile before judging |
| Bench matrix | =rg-bench-matrix run psi-baseline= | 30s CSV PASS | Collector pipeline **works** after PATH fix |

Honest summary: previous wide-net rows graded *candidates from literature and folklore*.
This table grades *what is true on the machine today*. Promotion gates still require
linux-rg A/B per =docs/rgx1gen11-targets.org=.

** Wide net pass 3 (2026-06-18 -- memory, network softirq, build-time)

| Candidate | Source | Layer | Grade | rgx1gen11 action |
| Meta bpfland / BBRv2 sysctl folklore | Meta production kernel tuning posts | sysctl | C | BBR already default (0003); revisit =net.ipv4.tcp_pacing_ss_low_latency= only on measured VPN regressions (=linux-rg-ervg=) |
| Cachy swappiness 150 + zram | CachyOS wiki, udev 30-zram.rules | policy | C | Chezmoi uses swappiness 100 + zram ram/2; A/B vs 150 only under =linux-rg-zram= bench, not blind port |
| memtierd | https://github.com/intel/memtierd | userspace daemon | C | Tiered DRAM/page-cache policy; overlaps DAMON reclaim (=CONFIG_DAMON_RECLAIM=y=); pick one primary reclaim actor (=linux-rg-71t0=) |
| DAMON reclaim | https://docs.kernel.org/admin-guide/mm/damon/reclaim.html | kernel module | B | Already pinned; =rg-damon-reclaim= toggles per profile (=linux-rg-tm76=) |
| KSM + MADV_MERGEABLE | kernel docs, Proxmox KSM wiki | kernel + QEMU | C | =CONFIG_KSM=y= pinned; enable merge only for explicit VM guests via =madvise=; watch scan CPU on 2C/4T P-cores (=linux-rg-pwdv=) |
| MGLRU min_ttl | https://docs.kernel.org/admin-guide/mm/multigen_lru.html | sysfs | C | =/sys/kernel/mm/lru_gen/min_ttl_ms=; tune only after vmscan weld bench (=linux-rg-5fxc=) |
| RPS / RFS on wlan0 | kernel scaling guide | netdev sysfs | C | Spread softirq across P-cores for AX211; measure before irqbalance disable (=linux-rg-i7w4=) |
| BBR + fq pacing | kernel ip-sysctl, Cloudflare BBR posts | sysctl + qdisc | B | Default stack; document =tcp_pacing_ss_low_latency= for interactive VPN (=linux-rg-ervg=) |
| AutoFDO / Propeller | kernel 6.13 docs, Phoronix 6.13 | build-time | D | skip; profile-guided -O3 on remote distribution builds only; no laptop runtime effect |
| systemd ManagedOOM + BPF | systemd.io MEMORY_PRESSURE | cgroup v2 | B | Chezmoi enables ManagedOOM on user slices; pairs PSI with Meta oomd layer (=linux-rg-oomd=) |
| irqbalance on single-package laptop | Garuda/Reddit stutter threads | userspace | C | Often harmful on 1365U; document disable test (=linux-rg-kqlk=) |

** Wide net pass 4 (graphics, suspend, TB4, platform ACPI)

| Candidate | Source | Layer | Grade | rgx1gen11 action |
| s2idle / S0ix validation | Intel S0ixSelftestTool, Fedora s0ix thread | suspend | B | Run =s0ix-selftest-tool.sh= post-boot (=linux-rg-t47k=); pair =linux-rg-slp= |
| Gen 11 standby drain | Reddit/NixOS Gen 11 threads, estada.ch fan-in-standby | firmware+kernel | C | BIOS 1.39 baseline; log =journalctl -b -1= after failed cycles (=linux-rg-slp=) |
| i915 vs xe PSR resume | Arch wiki Gen 11, Lunar Lake freeze reports | graphics | B | Chezmoi ships i915 modprobe; xe path tracked in =linux-rg-i915=; test PSR-off if resume glitches |
| boltctl authorize | Arch Thunderbolt wiki, kernel admin guide | userspace | B | TB4 dock: =boltctl list= / =boltctl enroll=; security policy for travel (=linux-rg-hzxn=) |
| TB4 retimer / dock DP | kernel thunderbolt sysfs | platform | C | =cat /sys/bus/thunderbolt/devices/*=; DDCCI gaps on docked DP (=linux-rg-ddcci=) |
| intel_vbtn + thinkpad_acpi | kernel thinkpad-acpi docs | kernel (pinned) | B | Fn keys, rfkill, charge thresholds via TLP (=linux-rg-wpdr=) |
| lenovo-wmi / think-lmi | Phoronix Think-LMI, Lenovo WMI docs | kernel/userspace | C | BIOS setting automation; reference only unless scripting fleet |
| btintel USB autosuspend | =CONFIG_BT_HCIBTUSB_AUTOSUSPEND=y= | kernel + USB | B | =linux-rg-bt= acceptance: pair/connect + one s2idle cycle |
| S0ixSelftestTool | https://github.com/intel/S0ixSelftestTool | userspace | B | Capture blocking devices list before blaming linux-rg patches (=linux-rg-t47k=) |

** Wide net pass 5 (Wi-Fi, audio, hibernate, virt edge)

| Candidate | Source | Layer | Grade | rgx1gen11 action |
| AX211 iwlwifi power_save | iwlwifi docs, Fedora AX211 driver thread | modprobe + profile | B | =rg-x1gen11-profile= travel vs docked; avoid stale driver mix (=linux-rg-iwl=) |
| iwlwifi bt_coex_active | =rgx1gen11-iwlwifi.conf= | modprobe | B | Already shipped for CNVi + USB BT (=linux-rg-iwl=, =linux-rg-bt=) |
| iwd roam logging | chezmoi iwd stack | userspace | B | =linux-rg-bench-wifi= |
| SOF Raptor Lake suspend | SOF intel_debug suggestions | kernel+firmware | B | =linux-rg-sof= + =linux-rg-slp=; watch dummy output after resume |
| PipeWire module-rt | PipeWire docs | userspace | B | =linux-rg-mvxn=; preferred over PREEMPT_RT |
| scx_lavd vs scx_flash | sched-ext.com, Cachy wiki | sched_ext | C | Profiles use lavd/rusty/asa; flash is burst tier only in ladder (=linux-rg-tjsl=) |
| hibernate + btrfs + LUKS swapfile | Arch PM wiki, Cachy hibernate thread | setup | C | Optional travel mode; btrfs swapfile caveats (=linux-rg-2ih0=) |
| MPTCP | mainline | network | D | Not part of the laptop kernel profile unless phone tether + Wi-Fi bench appears |
| Waydroid binder | Arch Waydroid wiki | kernel config | D | Rust binder pins already carried; no daily-driver Waydroid plan |
| Thread Director under KVM | not part of this profile | virt | D | Open only with a VM bench child |

** Wide net pass 6 (distro mines, power measurement, process hints)

| Candidate | Source | Layer | Grade | rgx1gen11 action |
| Arch wiki Gen 11 page | https://wiki.archlinux.org/title/Lenovo_ThinkPad_X1_Carbon_(Gen_11) | reference | A | Primary hardware quirk index; no wholesale port |
| NixOS nixos-hardware x1/11th-gen | GitHub nixos-hardware | reference | B | Module param ideas (TPM, audio); Gen 11-specific nix sparse -- cherry-pick ideas only |
| TLP vs PPD vs tuned | linrunner.de FAQ, Fedora tuned/ppd thread | policy | B | TLP primary (chezmoi disables PPD); tuned redundant (=linux-rg-pwr=) |
| power-profiles-daemon | GNOME/Fedora | userspace | C | skip; disabled intentionally; A/B only on test partition |
| RAPL / powercap | kernel powercap docs, powerjoular | observability | B | Package power logging for idle/build A/B (=linux-rg-sf6i=) |
| intel_pstate HWP EPP | kernel intel_pstate docs | sysfs | B | Profile should use =schedutil= + platform_profile; avoid global =performance= (=linux-rg-pwr=) |
| ananicy-cpp + Cachy rules | CachyOS ananicy-rules repo | userspace | D | After scx ladder pick (=linux-rg-db49=) |
| v4l2loopback | carried 0005 patch context | kernel module | C | Virtual camera path; not daily driver |
| Fedora kernel laptop QA | Fedora 6.13 test week, Lenovo Linux blog | reference | B | Watch Intel laptop fix churn on rebases -- not a patch port |
| ddcutil / ddcui | ddcutil.com | userspace | B | Docked external brightness (=linux-rg-ddcci=) |
| NVMe sanitize / wipefs | Arch SSD memory cell clearing wiki | maintenance | C | Pre-sale wipe only; not tuning -- document =nvme sanitize= vs =blkdiscard= |
| Gentoo power management guide | wiki.gentoo.org Power_management | reference | B | MSR/cpufreq trivia; same class as Framework power blogs |

** Wide net pass 7 (2026-06-18 -- validation gaps + literature-backed additions)

| Candidate | Source | Layer | Grade | rgx1gen11 action |
| thinkfan on Gen 11 | =/proc/acpi/ibm/fan= live probe | userspace | B | **Validated present**; test curve only if DPTF/TLP insufficient (=linux-rg-m00h=) |
| INTEL_HFI_THERMAL without HFI sysfs | stock =7.0.12= config | kernel | C | P/E split visible via cpufreq max; do not assume =cpu_capacity= differentiation (=linux-rg-tjsl=) |
| ElasticZRAM | Li et al. DAC 2024 (OokCite) | policy | A framing / D local | Swappiness+zram folklore now has peer-reviewed mobile framing; A/B vs chezmoi 100/ram2 (=linux-rg-zram=, =linux-rg-wr9r=) |
| IOSR swap I/O scheduling | Li et al. ACM TECS 2023 (OokCite) | kernel research | B | Informs zram writeback IO scheduling; no port without measurement (=linux-rg-wr9r=) |
| memtierd VM tiering | Prakash et al. arXiv 2025 (OokCite) | userspace | C | Literature for =linux-rg-71t0=; host-only laptop may not need tiering daemon |
| DAMON profiling origin | Park et al. Middleware 2019 (OokCite) | kernel | B | Foundational DAMON paper; reclaim is downstream module (=linux-rg-tm76=) |
| Whitaker HOTSTORAGE DOI | OokCite 10.1145/3599691.3603400 | literature | A | Confirms ADIOS skepticism; =linux-rg-rt1t= protocol |
| chezmoi PATH deployment | live doctor | ops | B | Symlink missing scripts to =~/.local/bin=; add chezmoi target or install script (=linux-rg-chzm=) |
| BORE scheduler | Phoronix/Cachy review; no DOI | patch | D | Still no peer review; highest-risk carry until bench-sched (=linux-rg-h2e3=, =linux-rg-bench-sched=) |

** Wide net pass 8 (2026-06-18 -- missed subsystems, live probes)

Areas absent from passes 1-7; each row includes a **live verdict** where probed.

| Candidate | Source | Layer | Grade | rgx1gen11 action |
| Synaptics Prometheus fingerprint | =lsusb 06cb:00fc=; fprint docs | userspace | C | **Present**; =fprintd= inactive (=linux-rg-1j07=); kernel-agnostic |
| UVC integrated camera | Chicony =04f2:b751=, =uvcvideo= | kernel+USB | C | **Loaded**; privacy/autosuspend policy (=linux-rg-al83=); thinkpad camera shutter via ACPI if exposed |
| VAAPI / media stack | Arch Intel graphics wiki, Jellyfin VA-API | userspace | C | =vainfo= not installed; probe after =mesa=; GuC/HuC (=linux-rg-r8jz=) |
| i915 GuC/HuC / FBC | i915 module params | modprobe | C | Params unread in probe (module built-in?); document for video call power (=linux-rg-r8jz=, =linux-rg-i915=) |
| btrfs =discard=async + LUKS | live =mount=, kernel cmdline | filesystem | B | **Active**: =discard=async= on LUKS mapper; =allow-discards= + =rd.luks.option=discard= in cmdline (=linux-rg-7pnt=) |
| fstrim.timer | systemd | maintenance | C | **Disabled** on host; pair with discard policy -- avoid double-trim confusion (=linux-rg-7pnt=) |
| =vm.dirty_* + writeback cadence | chezmoi =99-memory.conf= (not raw Arch) | sysctl | B | **Validated on disk**: ratio 10/5, =dirty_expire_centisecs=3000=, =dirty_writeback_centisecs=1500=; anti-spike rationale in template (=linux-rg-5x78=) |
| =vm.watermark_scale_factor=50= | chezmoi + kernel sysctl docs | sysctl | C | **Validated**; earlier reclaim under pressure |
| =vm.page-cluster=0= | Fedora/page-cluster threads | sysctl | C | **Validated**; pairs zram swap |
| =vm.vfs_cache_pressure=100= | sysctl | sysctl | C | **Validated** |
| NVMe multipath | =CONFIG_NVME_MULTIPATH=y= in overlay | kernel config | D | Single XG8 path; multipath **unnecessary** unless docked multi-path -- keep modular/off? |
| NVMe HWmon | =CONFIG_NVME_HWMON=y= | kernel | B | Thermal telemetry for =linux-rg-sf6i=; verify =/sys/class/hwmon= after boot |
| earlyoom vs systemd-oomd | HN comparisons | userspace | C | **earlyoom inactive**; oomd active -- do not stack without policy (=linux-rg-oomd=) |
| libinput palm detection | freedesktop libinput docs | compositor | C | Touchpad on =i2c_designware=; Sway/libinput quirks (=linux-rg-8mmb=) |
| TPM 2.0 | =/dev/tpm0= | platform | C | reference only; measured boot/PCR out of kernel-tuning scope unless LUKS tie-in wanted |
| intel-ucode | dmesg microcode revision | firmware | B | **0x00006134** loaded; track with =linux-firmware= / BIOS (=linux-rg-wc69=) |
| USB autosuspend | TLP + sysfs =power/control=auto= | USB PM | B | **auto** on probed devices; BT/fingerprint/camera candidates for stricter policy (=linux-rg-pwr=, =linux-rg-bt=, =linux-rg-al83=) |
| pcie_aspm.policy=powersave | kernel cmdline (live) | boot param | B | **Active** in cmdline; aligns with =CONFIG_PCIEASPM_*= pins (=linux-rg-wpdr=, =linux-rg-bench-idle=) |
| resume_offset hibernate | cmdline =resume=/dev/mapper/luks resume_offset=533760= | boot | C | **Partial hibernate wiring**; full path =linux-rg-2ih0=; validate after =linux-rg-slp= |
| pstore crash evidence | stock Arch pstore config + kernel docs | kernel/userspace | B | EFI pstore compiled in but disabled by default; backend and systemd-pstore/journald/rsyslog bridge tracked in =linux-rg-krgl= and =linux-rg-8da1= |
| tailscale / WireGuard | userspace VPN | network | C | **tailscaled active**; kernel =CONFIG_WIREGUARD= stock; BBR benefit on tailscale path =linux-rg-ervg= |
| swayidle | compositor idle | userspace | B | **Installed**; pairs =linux-rg-slp= and display power |
| journald persistent IO | systemd docs | userspace | C | Defaults not overridden; watch btrfs journal spam under build profile (=linux-rg-8da1=, =linux-rg-krgl=) |
| nftables firewall | chezmoi laptop power | userspace | B | reference only; Chezmoi ships rules; not kernel-tuned |
| Type-C / UCSI charging | =CONFIG_UCSI_ACPI=, =CONFIG_TYPEC_*= pins | kernel | B | Live: UCSI source 5V/3A sensor in thermal probe; dock PD =linux-rg-tb4= |
| SOF HDMI sink | =aplay -l= LEN L27i-28 | audio | B | **Dock monitor HDMI** enumerated on SOF card; hotplug =linux-rg-sof= + =linux-rg-tb4= |
| Mitigations sysfs | =/sys/devices/system/cpu/vulnerabilities/*= | security | B | reference only; **not affected** on all checked entries; no mitigations=off folklore |
| IOMMU / VT-d | dmesg | virt prep | C | Not validated in probe; enable only if KVM passthrough bench opens |
| NFC / WWAN | lspci | hardware | N/A | **Not present** on 21HM002GUS inventory |

** Search pattern index (=linux-rg-49gh= -- CLOSED 2026-06-18; validation continues under =linux-rg-e9fx=)

Status: **folklore mining exhausted** for rgx1gen11 daily-driver scope. **Live
validation and literature binding** continue under =linux-rg-e9fx=. Reopen wide-net
search on kernel major bump, new dock hardware, or failed =linux-rg-slp= /
=linux-rg-bench-wifi=.

| Pattern cluster | Queries run | Outcome |
| Thermals DPTF/TLP/thermald/PPD | thermald vs DPTF, TLP FAQ ppd, tuned vs TLP, Framework RAPL | Pass 2-3, 6; lane =linux-rg-m00h= |
| Memory zram/swap/reclaim | Cachy swappiness, memtierd vs DAMON, KSM madvise, MGLRU min_ttl, re-swappiness | Pass 3; lanes =linux-rg-zram=, =linux-rg-71t0=, =linux-rg-pwdv= |
| Network BBR/RPS/MPTCP | BBR pacing, RPS scaling, MPTCP laptop | Pass 3, 5; lanes =linux-rg-ervg=, =linux-rg-i7w4= |
| Storage NVMe/btrfs/hibernate | NVMe APST, KIOXIA XG8, btrfs hibernate LUKS | Pass 2, 5; lanes =linux-rg-2r1r=, =linux-rg-8s4g=, =linux-rg-2ih0= |
| Wi-Fi AX211/CNVi | iwlwifi AX211, bt_coex, iwd roam | Pass 2, 5; lanes =linux-rg-iwl=, =linux-rg-wifi= |
| Graphics/suspend | s2idle Gen 11, i915 PSR, S0ixSelftestTool, SOF Raptor Lake | Pass 4, 5; lanes =linux-rg-slp=, =linux-rg-i915=, =linux-rg-t47k= |
| TB4/dock | boltctl, thunderbolt sysfs, DDCCI | Pass 2, 4; lanes =linux-rg-tb4=, =linux-rg-hzxn=, =linux-rg-ddcci= |
| sched_ext/scx | scx_lavd vs flash, Cachy sched_ext wiki, ananicy | Pass 5, 6; lanes =linux-rg-tjsl=, =linux-rg-db49= |
| Observability | bpftrace, RAPL, S0ix tool, PSI/ManagedOOM | Pass 2-3, 6; lane =linux-rg-sf6i= |
| Platform ACPI/Lenovo | intel_vbtn, thinkpad_acpi, lenovo-wmi, btintel | Pass 4; lanes =linux-rg-bt=, =linux-rg-wc69= |
| Build-time / virt | AutoFDO/Propeller, Thread Director KVM, Waydroid | Pass 3, 5; not selected for laptop runtime tuning |
| Distro mines | Arch Gen 11, NixOS x1, Fedora kernel, Gentoo power guide | Pass 6; reference only |
| NVMe maintenance | wipefs, nvme sanitize, secure erase | Pass 6 supplement | Not part of tuning; maintenance doc only |
| Meta sysctl edge | tcp_notsent_lowat, pacing | Pass 3 supplement | Folded into =linux-rg-ervg= VPN notes |
| Chezmoi integration | rg-bench-matrix, rg-wide-net-collect, rg-nvme-apst, rg-hfi-status | Helpers shipped; lane =linux-rg-chzm= |
| Live validation | rg-x1gen11-profile doctor, rg-psi-snapshot, rg-bench-matrix | Pass 7-8 + =linux-rg-e9fx=; stock kernel baseline captured |
| ZRAM/swap literature | ElasticZRAM DAC 2024, IOSR TECS 2023, SWAM | OokCite validated; see =docs/literature-review.org= |
| Platform peripherals | fingerprint, UVC, TPM, touchpad, Type-C | Pass 8; lanes =linux-rg-1j07=, =linux-rg-al83=, =linux-rg-8mmb= |
| Graphics/media | VAAPI, GuC/HuC, i915 params | Pass 8; lane =linux-rg-r8jz= |
| Storage hygiene | discard async, fstrim, dirty_ratio, NVMe multipath | Pass 8; lanes =linux-rg-7pnt=, =linux-rg-5x78= |
| Session/VPN | swayidle, tailscale, nftables | Pass 8; chezmoi + =linux-rg-ervg= |
| Measured memory/IO | zram mm_stat, dirty sysctl, NVMe hwmon, zswap | Pass 9; =linux-rg-zram=, =linux-rg-5x78=, =linux-rg-7pnt= |
| sched_ext correction | stock sysfs state=disabled | Pass 9; =linux-rg-tjsl= |
| journald drop-in audit | persistent vs volatile conflict | Pass 9; =linux-rg-chzm= |

** Wide net pass 9 (2026-06-18 -- measured evidence + corrections)

Corrections to earlier probes and new quantified evidence.

| Candidate | Evidence source | Measured value | Grade | Action |
| sched_ext class on stock 7.0.12 | =CONFIG_SCHED_CLASS_EXT=y=, sysfs | =state=disabled=, =switch_all=0= | B | Earlier "sysfs missing" was **wrong**; linux-rg needs BPF scheduler load for A/B (=linux-rg-tjsl=) |
| zram compression ratio | =/sys/block/zram0/mm_stat= | **~3.68x** (4.9 GiB orig → 1.33 GiB stored) | B | Baseline for =linux-rg-zram=; compare after policy changes |
| zswap stack | module params | enabled=Y, compressor=**zstd** | B | Dual layer with zram; literature: Richard & Case 2014 (OokCite) (=linux-rg-wr9r=, =linux-rg-zram=) |
| NVMe composite temp | =nvme-pci-0400= hwmon | **49.9°C** idle-ish probe | B | Thermal headroom for =linux-rg-rt1t= / build bench |
| TLP battery policy | =tlp-stat -s= | thresholds **75/80**, profile balanced/BAT, **s2idle** | B | Validated; pairs =linux-rg-m00h= |
| Cgroup slices (chezmoi) | unit files on disk | browser **6G** high; build **12G** high / 20G max; rust-analyzer **10G/14G** | B | Documented caps; oomd is kill layer (=linux-rg-oomd=, =linux-rg-chzm=) |
| systemd-oomd timing | =/etc/systemd/oomd.conf.d/aggressive.conf= | **20s** pressure duration, **50%** user slice limit | B | Matches Meta PSI folklore tier (=linux-rg-oomd=) |
| iwlwifi firmware | =/lib/firmware/= | ma-b0-gf-a0 **-86/-89** + pnvm present | A | Record loaded rev at linux-rg boot (=linux-rg-wifi=) |
| Hibernate wiring | cmdline + sysfs | =resume=253:0=, =resume_offset=533760=; =/sys/power/state= includes **disk** | C | Swapfile offset wired; full cycle unvalidated (=linux-rg-2ih0=) |
| journald storage | drop-ins conflict | =99-persistent-storage.conf= vs =volatile-storage.conf=; runtime journal **8M** in =/run/log/journal= | C | **Conflict**: volatile may override; audit which wins (=linux-rg-chzm=) |
| i915 module params | =modinfo i915= | GuC/PSR params exist; sysfs values empty without modprobe args | C | Document modprobe.d for =linux-rg-r8jz= / =linux-rg-i915= |
| thinkpad thermal | =/proc/acpi/ibm/thermal= | temp **69** at probe | C | Supplement to sensors; fan speed 5665 RPM (=linux-rg-m00h=) |
| Camera shutter ACPI | thinkpad_acpi sysfs | **No** =camera_power_on= on Gen 11 | C | UVC software privacy only (=linux-rg-al83=) |
| DDCCI in-tree | =lsmod= | **Not loaded**; =intel_backlight= only | C | Module carried (0006); load for dock DDC (=linux-rg-ddcci=) |
| build.slice deployment | chezmoi vs home | Unit in chezmoi **12G/20G**; verify =systemctl --user= sees it | B | =rg-x1gen11-profile build-heavy= raises high water (=linux-rg-chzm=, =linux-rg-oomd=) |

** Chezmoi memory policy evidence (=99-memory.conf=, validated on disk 2026-06-18)

The production memory stack is **not** Arch defaults. Documented rationale from
=run_onchange_linux_memory_tuning.sh.tmpl=:

| Sysctl | Value | Rationale (chezmoi) |
| vm.swappiness | 100 | zram swap at RAM latency; prefer paging idle anon |
| vm.page-cluster | 0 | Finer-grained swap chunks for zram |
| vm.watermark_scale_factor | 50 | Earlier background reclaim |
| vm.min_free_kbytes | 262144 | Reserve headroom on 32 GiB box |
| vm.compaction_proactiveness | 0 | Reduce compaction stalls |
| vm.dirty_ratio / background | 10 / 5 | Cap dirty cache before flush storms |
| vm.dirty_expire_centisecs | 3000 | Shorter expire → smaller writeback bursts |
| vm.dirty_writeback_centisecs | 1500 | More frequent writeback worker wakeups |

Literature anchors: SWAM + ElasticZRAM + IOSR (OokCite) for mobile swap framing;
Richard & Case 2014 (OokCite 10.1016/j.diin.2014.05.011) for compressed swap
forensics. Local **3.68x** zram ratio is the empirical anchor until bench matrix
runs under =memory-pressure= profile.

Collectors (chezmoi, post-boot):

#+begin_src sh
rg-wide-net-collect              # snapshot for bench run dir or stdout
rg-nvme-apst /dev/nvme0          # APST + autopm sysfs
rg-hfi-status                    # cpu_capacity + intel_hfi
rg-bench-matrix run collect      # writes wide-net.txt into run dir
#+end_src

* Document index

- =docs/profiles.org= -- machine profiles index and applicability map
- =docs/rgx1gen11-targets.org= -- rgx1gen11 hardware inventory, workload profiles, targets
- =docs/rgx1gen11-kernel.org= -- rgx1gen11 profile and carried patch rationale
- =docs/literature-review.org= -- shared research bibliography and evidence grades
- =docs/candidates.org= -- this file (shared candidates + per-profile applicability)

Issue tracking: vissues project =linux-rg=, epic =linux-rg-ln3w=.
Hardware: =linux-rg-wpdr= and children in =docs/rgx1gen11-targets.org=.
Memory weld: =linux-rg-7zwz=, =linux-rg-5fxc=, =linux-rg-phgx=.
Scheduler: =linux-rg-kr7t= (cache-aware), =linux-rg-tjsl= (scx_loader).
Wide net: =linux-rg-49gh= (**CLOSED** 2026-06-18; search index exhausted); lanes
=linux-rg-m00h= thermals, =linux-rg-sf6i= observability, =linux-rg-2r1r= storage,
=linux-rg-ervg= network, =linux-rg-mvxn= PipeWire RT, =linux-rg-db49= ananicy,
=linux-rg-wc69= firmware, =linux-rg-59jn= bench matrix, =linux-rg-i7w4= RPS,
=linux-rg-71t0= memtierd, =linux-rg-pwdv= KSM, =linux-rg-2ih0= hibernate,
=linux-rg-hzxn= TB4 boltctl, =linux-rg-kqlk= irqbalance, =linux-rg-8s4g= NVMe APST,
=linux-rg-t47k= S0ix selftest, =linux-rg-1j07= fingerprint, =linux-rg-al83= UVC,
=linux-rg-r8jz= VAAPI, =linux-rg-7pnt= discard/fstrim, =linux-rg-5x78= dirty_ratio,
=linux-rg-8mmb= libinput.
