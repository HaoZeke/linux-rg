#+title: linux-rg rgx1gen11 kernel profile

* Document index

- =docs/rgx1gen11-kernel.org= (this file) -- machine profile and carried patches
- =docs/rgx1gen11-boot-safety.org= -- rollback and mkinitcpio rules (read first)
- =docs/rgx1gen11-drivers.org= -- Wi-Fi/BT/GPU/audio/NVMe driver stacks
- =docs/rgx1gen11-targets.org= -- verified hardware inventory, workload profiles,
  measurable targets, vissue map
- =docs/literature-review.org= -- evidence grades, venue map, why downstream
  patch stacks lack citations (read before carrying more cuts)
- =docs/candidates.org= -- ADIOS, re-swappiness, cpuidle TEO, detach_tasks,
  and other candidate or policy notes

Issue tracking (vissues):

- Vault: =Software/linux-rg/issues.org= under the obsidian-notes vault
- Epic: =linux-rg-ln3w= (Build linux-rg custom kernel for rgx1gen11)
- CLI: =vissue ready --root ~/Git/Gitlab/obsidian-notes --project linux-rg=

* Machine

Target machine:

- Lenovo ThinkPad X1 Carbon Gen 11, product `21HM002GUS`.
- CPU: Intel Core i7-1365U, 10 cores and 12 threads, Raptor Lake-P/U.
- GPU: Intel Raptor Lake-P Iris Xe Graphics.
- Network: Intel Raptor Lake PCH CNVi Wi-Fi.
- I/O: Thunderbolt 4, USB 3.2, Intel PCH I2C, NVMe.
- Storage: KIOXIA XG8 NVMe (1 TB class, PCI 1e0f:0010).
- RAM: 32 GiB (30 GiB visible); swap via zram and btrfs swapfile on stock system.

Verified live inventory and per-subsystem targets: =docs/rgx1gen11-targets.org=.

The package starts from Arch's `linux` package, not a generic vendor kernel.
The base source is kernel.org Linux plus the Arch patch release for
`7.0.12.arch1`. This keeps pacman packaging, module paths, mkinitcpio behavior,
and rEFInd installation compatible with the rest of the system.

* Patch Set

** BORE

Source: CachyOS kernel patches, `bore-cachy`.

Why: BORE biases the fair scheduler against burst-heavy tasks. That matches the
machine's interactive workload: Sway, browser sessions, terminals, editor
processes, background sync, and local builds share a 12-thread hybrid Intel
laptop CPU. The goal is lower desktop latency under mixed work, while keeping
Arch's fair scheduler fallback and normal Linux policy classes.

Fit: the upstream CachyOS hunk for `kernel/sched/fair.c` did not match the Arch
7.0 fair scheduler layout. `0002-bore-fair-arch-adapt.patch` carries only the
local placement fix for the rejected fair.c hunk. The full build compiles
`kernel/sched/bore.o` and the scheduler built-in archive.

Tradeoff: BORE is a downstream scheduler patch, not mainline Linux. This package
keeps `CONFIG_SCHED_CLASS_EXT=y` as well, so sched_ext userspace schedulers can
be tried without baking every scheduling decision into the kernel patch stack.
Kernel documentation describes sched_ext as a BPF-defined scheduler class that
can be turned on and off dynamically, and reverts to the fair scheduler on
detected scheduler errors:
https://docs.kernel.org/scheduler/sched-ext.html

** BBRv3

Source: CachyOS BBRv3 patch, adapted to Arch 7.0 TCP layout.

Why: BBR models bottleneck bandwidth and propagation delay instead of treating
loss as the primary congestion signal. The ACM Queue BBR paper describes the
model as estimating `BtlBw` and `RTprop` and notes that Linux uses fq pacing for
BBR operation:
https://queue.acm.org/detail.cfm?id=3022184

Fit: this laptop moves across Wi-Fi, airport networks, hotel networks,
Tailscale, SSH, and high-bandwidth lab networks. BBRv3 is selected as the
default TCP congestion control and fq as the default qdisc:

- `CONFIG_TCP_CONG_BBR=y`
- `CONFIG_DEFAULT_BBR=y`
- `CONFIG_DEFAULT_TCP_CONG="bbr"`
- `CONFIG_NET_SCH_FQ=y`
- `CONFIG_DEFAULT_FQ=y`
- `CONFIG_DEFAULT_NET_SCH="fq"`

Tradeoff: BBR fairness has real literature risk. Experimental work on BBRv1
reported RTT fairness problems, and later BBR revisions exist partly to address
such behavior. This package chooses BBRv3, not BBRv1, and keeps the kernel
configuration explicit so reverting to Cubic is a config change rather than a
patch-stack rewrite.

** v4l2loopback

Source: pf-kernel 7.0 patch carrying the v4l2loopback driver.

Why: the module supports virtual camera workflows without relying on an external
DKMS build every time the kernel changes. This matters on a custom kernel
because out-of-tree module drift is a common breakage point after kernel
upgrades.

Fit: `CONFIG_V4L2_LOOPBACK=m` keeps it loadable on demand. The package declares
`V4L2LOOPBACK-MODULE` and lists `v4l2loopback-utils` as optional userspace.

** DDCCI

Source: `ddcci-driver-linux`.

Why: DDCCI exposes monitor DDC/CI controls through kernel modules. It is useful
when the laptop is docked or attached to external displays and avoids rebuilding
the DDCCI module out of tree for this kernel.

Fit: `CONFIG_DDCCI=m` and `CONFIG_BACKLIGHT_DDCCI=m` keep support modular.
`ddcutil` remains userspace opt-in.

** Block layer contention patch

Source: CachyOS block patch from the 6.17 patch series (`0007-block-cachy.patch`).

Why: this patch reduces lock contention in BFQ and mq-deadline merge paths by
using try-lock behavior for expensive merge checks and by avoiding concurrent
BFQ dispatch. That matches the laptop's workload: browser caches, source trees,
package builds, VMs, and sync tools all share one NVMe device.

Fit: the patch applies cleanly to the Arch 7.0 source with offsets. BFQ is
enabled in the machine overlay:

- `CONFIG_IOSCHED_BFQ=y`
- `CONFIG_BFQ_GROUP_IOSCHED=y`

Tradeoff: the merge-path behavior can skip an occasional non-obvious merge
under contention. The patch comments state that the block plug remains the
successful merge point for related I/O. That is acceptable for this laptop
profile because the target is responsiveness under mixed interactive and build
load, not maximizing every synthetic merge opportunity.

Literature: BFQ has published scheduler research (Valente, ACM OSR 2012). The
contention patch itself has no published study. HOTSTORAGE 2023 questions
default mq scheduler use on ultra-low-latency NVMe; see
`docs/literature-review.org`.

** ADIOS I/O scheduler

Source: selected files from the CachyOS 6.17 omnibus patch (`0008-adios-cachy.patch`).

Why: ADIOS is an adaptive deadline mq scheduler for local NVMe testing. BFQ
remains the default because it is literature-backed and conservative for mixed
laptop I/O. ADIOS is built as a module so scheduler choice can be evaluated on
the KIOXIA XG8 without DKMS drift.

Fit:

- `CONFIG_MQ_IOSCHED_ADIOS=m`
- `# CONFIG_MQ_IOSCHED_DEFAULT_ADIOS is not set`

Operational and evaluation notes: `docs/candidates.org` (section "Carried: ADIOS
I/O scheduler").

Literature: no peer-reviewed ADIOS evaluation exists (evidence grade D). Carry
is justified as a modular experiment path, not as a literature-mandated default.

** Scheduler hot-path and hybrid CPU patches (0010-0013)

Source: sirlucjan =7.0/cachyos-fixes-patches-v27= cherry-picks plus Cachy sysctl
extract.

Why: the i7-1365U is a 12-thread hybrid CPU. These hunks target context-switch
latency, load-balance correctness, sched_ext wake locality, and desktop migration
cost without porting MUQSS or CONFIG_CACHY fair.

| Patch | Role |
| 0010 | =enter_lazy_tlb()=, =raw_spin_rq_unlock()=, =finish_task_switch()= inlines |
| 0011 | =detach_tasks()= loop_max fix under RQ lock |
| 0012 | sched_ext idle SMT sibling preference |
| 0013 | =sysctl_sched_migration_cost= 300000 ns default |

** Zen omnibus cherry-picks (0014-0016)

Source: Liquorix =zen/v7.0.12-lqx1.patch= (damentz/liquorix-package 7.0-13),
**not** the full omnibus or PDS scheduler.

Why: most zen driver hunks (iwlwifi AX211, i915 PSR/DP aux, intel_pstate hybrid
scaling) already match Arch 7.0.12. Two MM/schedutil cuts remain valuable on the
BORE stack without =CONFIG_ZEN_INTERACTIVE= or =CONFIG_SCHED_ALT=.

| Patch | Role |
| 0014 | =kswapd_waiters=: keep kswapd awake while page allocators wait |
| 0015 | schedutil DL path uses =limits_changed= for bandwidth spikes |
| 0016 | cpuidle TEO default over menu (live bench had menu governor) |

Skipped from zen: PDS/BMQ alt scheduler, fair.c ZEN_INTERACTIVE sysctl (0013
covers migration_cost), duplicate 0010 inlines, CONFIG_ZEN_INTERACTIVE elevator
and swap tuning. See =docs/candidates.org= Liquorix section for the full matrix.

** ASA sched_ext router (0009)

Source: arXiv:2511.11628 prototype; =asa-router= userspace helper.

Why: reversible sched_ext expert switching (interactive/cpu/io/mixed) without
replacing EEVDF fair class. Pairs with =CONFIG_SCHED_CLASS_EXT=y= on hybrid CPUs.

Fit: =CONFIG_SCHED_ASA=y=, sysfs under =/sys/kernel/sched_ext/asa/=.

** PSI and memory pressure

Source: mainline Linux configuration.

Why: PSI gives userspace a direct pressure signal for memory, CPU, and I/O. It
is the kernel-side foundation needed by pressure-aware userspace policy such as
`oomd`. The profile pins PSI, memcg, memcg swap accounting, cgroups, and BPF.

Fit:

- `CONFIG_PSI=y`
- `CONFIG_MEMCG=y`
- `CONFIG_MEMCG_SWAP=y`
- `CONFIG_CGROUP_BPF=y`

** DAMON

Source: mainline Linux configuration.

Why: DAMON is upstream kernel work for low-overhead memory access monitoring
and access-aware operations. It fits the X1 profile because the machine runs
browsers, containers, local builds, VMs, and scientific tools where pressure
can come from stale anonymous memory and large file-backed working sets.

Fit:

- `CONFIG_DAMON=y`
- `CONFIG_DAMON_VADDR=y`
- `CONFIG_DAMON_PADDR=y`
- `CONFIG_DAMON_SYSFS=y`
- `CONFIG_DAMON_RECLAIM=y`
- `CONFIG_DAMON_LRU_SORT=y`
- `CONFIG_DAMON_STAT=y`
- `# CONFIG_DAMON_STAT_ENABLED_DEFAULT is not set`

The policy is intentionally not enabled by default. The kernel exports the
monitoring and action mechanisms, while systemd units can turn on bounded
reclaim or LRU sorting only for the laptop modes where that is useful. The
kernel documentation describes DAMON as efficient data-access monitoring with
access-aware operations, including reclaim and LRU-list sorting:
https://docs.kernel.org/admin-guide/mm/damon/index.html

** MGLRU, KSM, zswap, and zram

Source: mainline Linux configuration.

Why: the laptop has enough RAM for normal work but still benefits from pressure
handling during browsers, local builds, containers, VMs, and scientific
toolchains. MGLRU improves page reclaim behavior, zswap/zram provide compressed
memory paths, and KSM can deduplicate selected anonymous pages.

Fit:

- `CONFIG_LRU_GEN=y`
- `CONFIG_LRU_GEN_ENABLED=y`
- `CONFIG_KSM=y`
- `CONFIG_ZSWAP=y`
- `CONFIG_ZSWAP_DEFAULT_ON=y`
- `CONFIG_ZRAM=m`

The kernel KSM documentation describes KSM as a `CONFIG_KSM=y` feature that
deduplicates anonymous pages registered with `MADV_MERGEABLE`; it also documents
the scan-time advisor and profit metrics:
https://docs.kernel.org/admin-guide/mm/ksm.html

The kernel MGLRU documentation exposes `/sys/kernel/mm/lru_gen/enabled`; the
profile enables the feature by default:
https://docs.kernel.org/admin-guide/mm/multigen_lru.html

UKSM is not selected. The selected source sets do not carry a maintained 7.0
UKSM patch. CachyOS provides older KSM/UKSM-era patches and a userspace KSM
settings package, while Linux 7.0 already has KSM smart scan, scan-time advisor
controls, MGLRU, zswap, zram, and PSI. Carrying a stale memory-management patch
would add higher risk than benefit for this laptop.

** Battery and laptop hardware

Source: Arch config plus machine overlay.

Why: the profile keeps Intel and ThinkPad laptop paths visible to the config
resolver. That includes Intel pstate, Intel idle, schedutil, ThinkPad ACPI,
platform profile, Intel PMC/PMT telemetry, Intel Wi-Fi, Intel Bluetooth, i915,
xe, SOF audio, NVMe, USB4, Thunderbolt, Type-C, I2C HID, and ACPI suspend and
hibernation support.

Fit:

- `CONFIG_X86_INTEL_PSTATE=y`
- `CONFIG_INTEL_IDLE=y`
- `CONFIG_CPU_FREQ_DEFAULT_GOV_SCHEDUTIL=y`
- `CONFIG_THINKPAD_ACPI=m`
- `CONFIG_ACPI_PLATFORM_PROFILE=y`
- `CONFIG_PCIEASPM_POWER_SUPERSAVE=y`
- `CONFIG_PM_AUTOSLEEP=y`
- `CONFIG_HIBERNATION=y`
- `CONFIG_SUSPEND=y`

* Carry Rules

Patches are kept only when they satisfy all of these constraints:

- Source is public and reviewable.
- The patch applies reproducibly from the PKGBUILD.
- The code compiles against the selected Arch kernel base.
- The behavior matches this ThinkPad profile.
- The feature can be disabled or made modular when practical.

This keeps the package closer to Arch than to a large omnibus kernel. CachyOS
and pf-kernel are source pools, not wholesale dependencies.

* Candidate Matrix

Summary table. Evidence grades and evaluation plans are in `docs/candidates.org`
and `docs/literature-review.org`.

| Area | Candidate | Source | Decision | Evidence | Reason |
| Scheduler | BORE | CachyOS 7.0 | carried | D | Compiles on Arch 7.0; no peer-reviewed evaluation. |
| Scheduler | Prjc/BMQ/PDS | CachyOS 7.0 | not carried | D | Conflicts with BORE plus sched_ext. |
| Scheduler | sched_ext schedulers | mainline plus scx userspace | userspace | B | See literature review; use `scx_loader`. |
| Scheduler | hot-path inlines 0010 | cachyos-fixes #06-08 | carried | B/D | Context-switch path; hybrid laptop fit |
| Scheduler | detach_tasks fix 0011 | cachyos-fixes #09 | carried | B | Hybrid load-balance; fair.o builds with BORE |
| Scheduler | sched_ext SMT idle 0012 | cachyos-fixes #18 | carried | B | 2-way SMT laptop wake locality |
| Scheduler | migration_cost 0013 | Cachy sysctl extract | carried | D | Desktop latency knob with BORE |
| MM | kswapd_waiters 0014 | Liquorix zen omnibus | carried | D | Memory-pressure responsiveness; boot-safe |
| CPUfreq | schedutil limits 0015 | Liquorix zen omnibus | carried | D | Hybrid DL bandwidth on 1365U |
| Power | cpuidle TEO 0016 | sirlucjan / upstream Loehle | carried | B | Live probe: menu was default |
| Rust | binder + fw_loader | Arch 7.0.12 mainline | carried (config) | B/C | iwlwifi/SOF firmware path |
| Scheduler | ASA router 0009 | arXiv:2511.11628 | carried (prototype) | D | sched_ext layer; needs scx_loader |
| Scheduler | newidle balance revert | cachyos-fixes-v27 #05 | not carried | D | Conflicts with BORE. |
| Network | BBRv3 | CachyOS | carried | A | Roaming paths; fairness caveats documented. |
| Block | BFQ/mq-deadline contention | CachyOS 6.17 | carried | A/D | BFQ literature strong; patch and ULL NVMe benefit uncertain. |
| Block | ADIOS | CachyOS 6.17 omnibus | carried (module) | D | Non-default; NVMe A/B testing. |
| Memory | Cachy anon/clean ratios | Cachy 6.17 extract | candidate | B/D | Weld with mglru-patches-v2; see candidates.org |
| Memory | mglru-patches-v2 | sirlucjan 7.0 | candidate | D | vmscan dev-tree; weld with ratios not re-swappiness |
| Memory | re-swappiness v1.2 | firelzrd / sirlucjan 7.0 | candidate | D | Deprioritized vs mglru+ratios; conflicts mglru on vmscan |
| Scheduler | cache-aware SCHED_CACHE | sirlucjan 7.0 | candidate | D | Applies on BORE stack; hold behind scx_loader |
| Memory | DAMON reclaim/LRU sort/stat | mainline Linux | config | B | Policy in systemd units. |
| Memory | UKSM | CachyOS KSM-era | not carried | D | No maintained 7.0 patch. |
| Power | cpuidle TEO over menu | sirlucjan 7.0 | candidate | B | Intel laptop idle; check Arch default first. |
| Policy | systemd-oomd | Meta PSI/OOMD | userspace | B/C | Pairs with CONFIG_PSI. |
| GPU memory | cgroup VRAM | CachyOS 7.0 | not carried | D | iGPU laptop; patch does not apply cleanly. |
| Graphics RT | rt-i915 | CachyOS 7.0 | not carried | D | PREEMPT_RT not enabled. |
| Media | v4l2loopback | pf-kernel 7.0 | carried | D | DKMS avoidance. |
| Display | DDCCI | ddcci-driver-linux | carried | D | DKMS avoidance for docked displays. |
| KSM policy | KSM service/settings | CachyOS userspace | userspace | B | Activation in systemd policy. |
| Power profiles | scx_loader switching | CachyOS PPD | userspace | B | Scheduler mode in power-profile policy. |
| Handheld | handheld patch | CachyOS 7.0 | not carried | D | Wrong hardware class. |
| Filesystem | aufs | CachyOS 7.0 | not carried | D | Not a laptop performance feature. |
| ACPI | acpi_call | CachyOS 7.0 | not carried | D | Risky; not required on ThinkPad. |
| Compiler | clang-polly | CachyOS 7.0 | not carried | D | Arch GCC packaging path. |
| Hardening | hardened | CachyOS 7.0 | not carried | D | Compatibility and performance risk. |
