#!/usr/bin/env bash
# Selftest for linux-rg-criu-check.
set -euo pipefail
repo=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)
check=$repo/scripts/linux-rg-criu-check
fail() { printf 'FAIL: %s\n' "$*" >&2; exit 1; }

"$check" || fail "overlays must pass"
tmp=$(mktemp)
trap 'rm -f "$tmp"' EXIT
printf 'CONFIG_CHECKPOINT_RESTORE=y\n' >"$tmp"
"$check" "$tmp" && fail "sparse config must fail" || true
echo ok
