diff -ruN a/rtl88xxau-r1314.7344855/include/osdep_service_linux.h b/rtl88xxau-r1314.7344855/include/osdep_service_linux.h
--- a/rtl88xxau-r1314.7344855/include/osdep_service_linux.h	2026-06-26 17:35:01.000000000 -0500
+++ b/rtl88xxau-r1314.7344855/include/osdep_service_linux.h	2026-08-19 18:32:06.075372512 -0500
@@ -57,6 +57,7 @@
 #include <linux/ip.h>
 #include <linux/kthread.h>
 #include <linux/list.h>
+#include <linux/timer.h>
 #include <linux/vmalloc.h>
 
 #if (LINUX_VERSION_CODE <= KERNEL_VERSION(2, 5, 41))
@@ -356,7 +357,11 @@
 #endif
 {
 #if (LINUX_VERSION_CODE >= KERNEL_VERSION(4, 14, 0))
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 16, 0))
+	_timer *ptimer = timer_container_of(ptimer, in_timer, timer);
+#else
 	_timer *ptimer = from_timer(ptimer, in_timer, timer);
+#endif
 #else
 	_timer *ptimer = (_timer *)cntx;
 #endif
@@ -385,7 +390,11 @@
 
 __inline static void _cancel_timer(_timer *ptimer, u8 *bcancelled)
 {
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 16, 0))
+	*bcancelled = timer_delete_sync(&ptimer->timer) == 1 ? 1 : 0;
+#else
 	*bcancelled = del_timer_sync(&ptimer->timer) == 1 ? 1 : 0;
+#endif
 }
 
 static inline void _init_workitem(_workitem *pwork, void *pfunc, void *cntx)
diff -ruN a/rtl88xxau-r1314.7344855/Makefile b/rtl88xxau-r1314.7344855/Makefile
--- a/rtl88xxau-r1314.7344855/Makefile	2026-06-26 17:35:01.000000000 -0500
+++ b/rtl88xxau-r1314.7344855/Makefile	2026-08-19 18:32:06.074924252 -0500
@@ -1661,6 +1661,8 @@
 ifneq ($(KERNELRELEASE),)
 
 ########### this part for *.mk ############################
+ccflags-y += $(EXTRA_CFLAGS)
+
 include $(src)/hal/phydm/phydm.mk
 
 rtk_core :=	core/rtw_cmd.o \
diff -ruN a/rtl88xxau-r1314.7344855/os_dep/linux/ioctl_cfg80211.c b/rtl88xxau-r1314.7344855/os_dep/linux/ioctl_cfg80211.c
--- a/rtl88xxau-r1314.7344855/os_dep/linux/ioctl_cfg80211.c	2026-06-26 17:35:01.000000000 -0500
+++ b/rtl88xxau-r1314.7344855/os_dep/linux/ioctl_cfg80211.c	2026-08-19 18:32:06.077811087 -0500
@@ -3365,8 +3365,15 @@
 	return ret;
 }
 
-static int cfg80211_rtw_set_wiphy_params(struct wiphy *wiphy, u32 changed)
+static int cfg80211_rtw_set_wiphy_params(struct wiphy *wiphy,
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 17, 0))
+	int radio_idx,
+#endif
+	u32 changed)
 {
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 17, 0))
+	(void)radio_idx;
+#endif
 #if 0
 	struct iwm_priv *iwm = wiphy_to_iwm(wiphy);
 
@@ -4267,12 +4274,18 @@
 #if (LINUX_VERSION_CODE >= KERNEL_VERSION(3, 8, 0))
 	struct wireless_dev *wdev,
 #endif
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 17, 0))
+	int radio_idx,
+#endif
 #if (LINUX_VERSION_CODE >= KERNEL_VERSION(2, 6, 36)) || defined(COMPAT_KERNEL_RELEASE)
 	enum nl80211_tx_power_setting type, int mbm)
 #else
 	enum tx_power_setting type, int dbm)
 #endif
 {
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 17, 0))
+	(void)radio_idx;
+#endif
 
 _adapter *padapter = wiphy_to_adapter(wiphy);
 HAL_DATA_TYPE   *pHalData = GET_HAL_DATA(padapter);
@@ -4328,11 +4341,17 @@
 #if (LINUX_VERSION_CODE >= KERNEL_VERSION(3, 8, 0))
 	struct wireless_dev *wdev,
 #endif
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 17, 0))
+	int radio_idx,
+#endif
 #if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 14, 0))
 	unsigned int link_id,
 #endif
 	int *dbm)
 {
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(6, 17, 0))
+	(void)radio_idx;
+#endif
 	_adapter *padapter = wiphy_to_adapter(wiphy);
 	HAL_DATA_TYPE *pHalData = GET_HAL_DATA(padapter);
 

--- a/rtl88xxau-r1314.7344855/core/rtw_br_ext.c	2026-06-26 17:35:01.000000000 -0500
+++ b/rtl88xxau-r1314.7344855/core/rtw_br_ext.c	2026-08-23 08:20:29.442365661 -0500
@@ -22,6 +22,12 @@
 	#include <linux/udp.h>
 	#include <linux/if_pppox.h>
 #endif
+#if defined(__KERNEL__) && (LINUX_VERSION_CODE >= KERNEL_VERSION(7, 1, 0))
+/* 7.1 hides pppoe_tag.tag_data from kernel builds. Payload follows the header. */
+#define rtw_pppoe_tag_data(tag) ((char *)((tag) + 1))
+#else
+#define rtw_pppoe_tag_data(tag) ((tag)->tag_data)
+#endif
 
 #if 1	/* rtw_wifi_driver */
 	#include <drv_types.h>
@@ -953,8 +959,8 @@
 								return -1;
 							}
 
-							memcpy(tag->tag_data + MAGIC_CODE_LEN + RTL_RELAY_TAG_LEN,
-							       pOldTag->tag_data, old_tag_len);
+							memcpy(rtw_pppoe_tag_data(tag) + MAGIC_CODE_LEN + RTL_RELAY_TAG_LEN,
+							       rtw_pppoe_tag_data(pOldTag), old_tag_len);
 
 							if (skb_pull_and_merge(skb, (unsigned char *)pOldTag, TAG_HDR_LEN + old_tag_len) < 0) {
 								DEBUG_ERR("call skb_pull_and_merge() failed in PADI/R packet!\n");
@@ -967,9 +973,9 @@
 						tag->tag_len = htons(MAGIC_CODE_LEN + RTL_RELAY_TAG_LEN + old_tag_len);
 
 						/* insert the magic_code+client mac in relay tag */
-						pMagic = (unsigned short *)tag->tag_data;
+						pMagic = (unsigned short *)rtw_pppoe_tag_data(tag);
 						*pMagic = htons(MAGIC_CODE);
-						memcpy(tag->tag_data + MAGIC_CODE_LEN, skb->data + ETH_ALEN, ETH_ALEN);
+						memcpy(rtw_pppoe_tag_data(tag) + MAGIC_CODE_LEN, skb->data + ETH_ALEN, ETH_ALEN);
 
 						/* Add relay tag */
 						if (__nat25_add_pppoe_tag(skb, tag) < 0)
@@ -1030,14 +1036,14 @@
 						return -1;
 					}
 
-					pMagic = (unsigned short *)tag->tag_data;
+					pMagic = (unsigned short *)rtw_pppoe_tag_data(tag);
 					if (ntohs(*pMagic) != MAGIC_CODE) {
 						DEBUG_ERR("Can't find MAGIC_CODE in %s packet!\n",
 							(ph->code == PADO_CODE ? "PADO" : "PADS"));
 						return -1;
 					}
 
-					memcpy(skb->data, tag->tag_data + MAGIC_CODE_LEN, ETH_ALEN);
+					memcpy(skb->data, rtw_pppoe_tag_data(tag) + MAGIC_CODE_LEN, ETH_ALEN);
 
 					if (tagLen > MAGIC_CODE_LEN + RTL_RELAY_TAG_LEN)
 						offset = TAG_HDR_LEN;
